# HRL (Halo Race Leaderboard) — Agent Handoff

Orientation doc for any AI agent (Claude Code, Codex, or otherwise) working in this repo. This file is intentionally short — it tells you what the project is and where to find the details. **This "Current state" section is the project's living handover doc — update it, and `docs/roadmap.md`, at the end of every task, not just at the end of a session.** Also keep everything else in `docs/` current: when you make an architectural decision, resolve an open item, hit and fix a real bug, or change the plan, update the relevant doc before finishing — don't just report it in chat and let the docs go stale. See [docs/decisions.md](docs/decisions.md) for the standing format (chronological, includes reversals and incidents, not just current-state facts).

## What this is

Rebuilding the frontend and API for a public Halo racing leaderboard (HRL). Existing app: https://github.com/EffakT/HRL-Web-Interface, currently live in production. The **database is preserved** (real prod data imported into dev), the **application is reinstalled fresh** on Laravel 13 + Livewire + Tailwind. Full detail: [docs/scope.md](docs/scope.md).

## Current state (at a glance)

**Just done (2026-07-10)**: a real live-production question ("why does server X show offline when it's actually up?") uncovered and fixed a genuine ops bug — `schedule:work` had silently stopped invoking `RefreshLiveServerInfo` for ~23 hours, with no error anywhere, because a `withoutOverlapping()` scheduler mutex got stuck in the `cache_locks` table. Restarting the process didn't help (the lock lives in the DB, not the process) — only deleting the stale row did, confirmed live. Fixed two ways, both discussed with the user first: removed `->withoutOverlapping()` entirely (this job is idempotent and cheap, so the mutex was pure downside), and added `App\Console\Commands\CheckSchedulerHealth`, a deliberately independent watchdog registered via a **real crontab entry** (not `Schedule::`) that alerts if `queried_at` goes stale — independent on purpose, since a check that depends on the same scheduler would never fire when it's actually needed. Also found and fixed a real bug building it: Carbon 3 (this app's Laravel 13 dependency) made `diffInMinutes()` signed by default, so a straightforward staleness check silently never tripped until caught by the command's own tests. Separately re-confirmed this app's user *does* have working crontab access, correcting an earlier (2026-07-08) "no crontab" finding that had never been re-checked since. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/deployment.md](docs/deployment.md).

**Earlier (2026-07-10)**: closed QUAL-01 (tooling/documentation baseline). `composer check` (Pint + PHPStan + Rector dry-run + Pest) is now a genuinely clean gate for the first time — Rector's dry-run had quietly accumulated a 66-file backlog (up from 46 at the original audit) since it was configured but never actually run to completion. Reviewed every proposed change by rule category before applying for real (not a blind `rector process`), given a past incident in this project where an unreviewed mechanical fix shipped a real regression (see below) — checked the higher-risk categories individually: `EloquentOrderByToLatestOrOldestRector`'s `orderBy('time')->oldest()` rewrites are semantically identical to the original `orderBy('time')->orderBy('created_at')` (`oldest()` defaults to ascending `created_at`, same as the omitted-direction original); `DispatchToHelperFunctionsRector`'s `X::dispatch(...)` → `event(new X(...))` is byte-for-byte what `Dispatchable::dispatch()` already does under the hood (confirmed by reading the trait); the `PlayerClaim`/`ServerClaim` `protected $table` → `#[Table(name: ...)]` attribute conversion is a real, supported Laravel 13 Eloquent attribute, not a guess. Verified clean afterward: 217 tests (216 passing, 1 pre-existing skip) / 662 assertions, Pint clean, PHPStan level 8 clean, Rector dry-run now reports 0 changed files. Also removed 4 `*.php-legacy` reference files (`QueryServer`, `ProcessNewLap`, `ProcessPlayerClaim`, `ApiController`) and 2 stock Laravel example tests — all fully recoverable via git history, confirmed with the user first since deleting files (even git-tracked ones) is an irreversible-feeling action the auto-mode classifier correctly paused on. Fixed several stale documentation claims the audit's second half flagged — `docs/architecture.md`/`docs/scope.md`/`docs/security.md`/`docs/coding-standards.md`/`docs/performance.md` all still described real, shipped features (Reverb/Echo, the read API, input validation, every frontend page) as "planned, not yet built" or "still mock data," a leftover from the original Phase 1 planning docs never being revisited once Phase 2 shipped; also corrected a live data-count claim (performance.md said ~1,942 laps/823 players, real count is 1,682/832) and a genuine factual drift (performance.md claimed Most Active Server was built with periodic recalculation + caching; it actually shipped computed-fresh-on-every-call, same as `GlobalRanking` — the doc never got updated to match the real shipped decision). `docs/testing.md` now has one clearly-dated "Current baseline" line rather than the stale in-paragraph test count the audit specifically called out. Full detail in [docs/decisions.md](docs/decisions.md).

**Earlier (2026-07-10)**: closed SEO-01 (minimal metadata). The layout only had a title/viewport before — no descriptions, canonical URLs, OG/Twitter tags, structured data, or sitemap, and detail pages used generic titles (`Player | Halo Race Leaderboard`) with no entity name. Two things in the finding were genuinely open-ended and put to the user rather than decided solo: whether this pre-launch redesign environment should be indexed at all (user chose **block for now, config-driven** — `config('seo.allow_indexing')`, `SEO_ALLOW_INDEXING` env, default `false`; a sitewide `noindex` meta tag and `Disallow: /` in `/robots.txt` while false, flip one value at launch), and what the sitemap should enumerate (user chose **top-level entities only** — servers/maps/players index + detail pages, not the nested `/servers/{id}/maps/{id}`/`/servers/{id}/players/{id}` routes, which are variant views of the same content). Layout (`components/layout.blade.php`) gained a canonical `<link>`, OG/Twitter meta, and the conditional robots meta. All five detail-page Livewire components (`PlayerShow`, `ServerShow`, `ServerPlayerShow`, `MapLeaderboard`, `ServerMapLeaderboard`) now pass a real dynamic title/description via Livewire's `->layoutData([...])` view macro rather than the static `#[Layout(...)]` attribute (PHP attribute args must be compile-time constants, so they can't interpolate an entity name — confirmed by reading `SupportPageComponents.php`: a `render()`-time `layoutData()` call runs before the attribute's own params get merged in, and `PageComponentConfig::mergeParams()` always lets the earlier call's keys win on conflict, so this doesn't fight the attribute). List/static pages got static descriptions. `/robots.txt` is now a route (`RobotsController`), not a static `public/robots.txt` file — needed removing the static file first, since a webserver serves that directly before Laravel ever sees the request. `/sitemap.xml` (`SitemapController`) builds real XML directly with `SimpleXMLElement` rather than a Blade view — a literal `<?xml ...?>` declaration inside a `.blade.php` file trips Blade's raw-PHP-tag compiler guard and silently never compiles (confirmed via `Blade::compileString()` — the `{{ }}` line came back completely unconverted). Manually verified live against the staging site (title/description/canonical/OG/robots meta on every page type including both nested server-scoped routes, `/robots.txt` disallowing, `/sitemap.xml` real content). Full detail in [docs/decisions.md](docs/decisions.md).

**Earlier (2026-07-10)**: closed A11Y-01 (keyboard/modal accessibility). Podium cards (`leaderboard-podium-and-table.blade.php`) were plain `<div wire:click>` with no keyboard support — converted to real `<button type="button">`, matching the table rows which already were buttons. Both lap modals (`lap-detail-modal.blade.php`, `lap-vs-record-modal.blade.php`) gained `role="dialog"`/`aria-modal="true"`/`aria-labelledby`, an Escape handler, and a real focus trap + restore-on-close via the official `@alpinejs/focus` plugin (`x-trap` directive) — a new dependency, approved by the user first per this repo's "no dependency changes without approval" rule. Also added a skip-to-main-content link in `layout.blade.php`, a global `:focus-visible` outline, and a `prefers-reduced-motion` media query. `LeaderboardModalTest.php` gained real keyboard-interaction coverage (Enter to open, Escape to close); manually re-verified live against the staging site with a throwaway Playwright script confirming the skip link, dialog semantics, the focus trap actually holding focus, and focus restoration on close. **User caught a real gap in that verification immediately after**: the focus ring wasn't actually visible on the podium cards — `.hud-clip`/`.hud-clip-sm`'s `clip-path` (the corner-clipped HUD panel motif) clips away anything painted outside the element's own shape, including its own outline, since the default `:focus-visible` outline paints outside the border box. Fixed with a `-3px` outline-offset scoped to those two classes, pulling the ring inside the shape instead; reverified live (podium card and modal close button both now show a visible ring on focus). Full detail in [docs/testing.md](docs/testing.md) and [docs/decisions.md](docs/decisions.md).

**Earlier (2026-07-10)**: two bugs found and fixed in `hrl-refactored.lua` (a separate Lua script outside this repo — not live yet, but will replace the current production `hrl.lua` when this redesign launches), plus one standing decision. User reported holding the only lap record on a map at 69.37s, then finishing a slower 90.57s lap that was still broadcast as "a new lap record" — root cause: the "new record" message was gated on `leaderboardPosition.position == 1` alone, true for *any* lap by the existing #1 holder even a slower one; the server already returns `isNewRecord`/`lapTime` correctly, the Lua client just never read them (now fixed in `hrl-refactored.lua`; the currently-live `hrl.lua` still has the identical bug until launch). Second: debugged `hrl-refactored.lua`'s own attempted fix for the known last-lap-not-recorded bug (`LAST-LAP-BUG.md` at the repo root) and found it still had the same race it was trying to fix — `LapTracker:on_game_end()` unconditionally reset every player's checkpoint state in the same loop it submitted completed laps from, so a winning lap's `EVENT_SCORE` still in flight when `EVENT_GAME_END` fires got wiped before it could be processed. Fixed by removing that premature reset (genuinely incomplete laps, e.g. a skip vote, are still never submitted — cleaned up naturally by the next `OnGameStart`/`OnPlayerJoin` instead) — this fix **will** be live once `hrl-refactored.lua` ships with the redesign. **Also decided: Rally support is a no-go**, independent of the last-lap fix landing — even though that fix incidentally resolves Rally's own root cause too, Rally isn't being pursued further and no Rally golden fixture is planned. **Correction, same session: Any Order is not actually blocked.** The 2026-07-09 investigation diagnosed Any Order's bug in the currently-deployed legacy `hrl.lua` (`NormalizeCheckpointId()`'s `log2()`-based decode only works for contiguous checkpoint bitmasks) — but `hrl-refactored.lua` never used that approach at all: `decode_checkpoint_bits()` walks every individual bit regardless of contiguity, so Any Order already works correctly there, same "broken in the old script, already fixed in the new one" pattern as the other two fixes above. Updated Home's "Known Issues" copy accordingly: removed both the final-lap and Any Order bullets (both fixed by launch), and reworded Rally from "doesn't currently work" to "not currently supported" (a standing decision, not a pending bug fix). Full detail in [LAST-LAP-BUG.md](../../LAST-LAP-BUG.md), [docs/decisions.md](docs/decisions.md), [docs/roadmap.md](docs/roadmap.md), and [docs/testing.md](docs/testing.md).

**Earlier (2026-07-09)**: TEST-01 is now fully closed. PHPStan raised to level 8 (from 7) — ~35 of 46 errors were one false positive (`LapTime`'s FK-enforced relations, fixed via a class-level `@property-read` docblock instead of 35 null-guards), ~11 were real individually-judged null-safety fixes. Real coverage evidence exists (PCOV installed, 94% line coverage) and a dedicated `redesign_hrl_test` MySQL database now backs the concurrency test instead of the real DB. Mutation testing installed (Pest's native `--mutate`, not Infection, which doesn't support Pest's bootstrap) — found and fixed a real gap in `LapSubmissionHash` (70.83% → 95.83%, split-field/`hrl_token` changes weren't asserted to affect the security-relevant content hash); this box's 6GB RAM can only run mutation testing one class at a time (crashed twice at namespace scope). A code review then found the MySQL concurrency test could pass without proving the lock actually mattered — fixed in three layers (a real synchronization barrier, closing the barrier-timeout's own coverage hole, and a deliberate 300ms delay forcing genuine overlap inside the critical section), verified by temporarily removing the lock and confirming the test fails, then adding a permanent negative-control test proving the harness itself would catch a future regression. Finally, built the real end-to-end Echo/Reverb browser delivery test (`tests/Browser/EchoReverbDeliveryTest.php`) — the one gap nothing else could close, since calling a listener method directly skips Echo and the earlier manual `pusher-js` script bypassed Echo/Livewire on purpose. Deliberately runs against real production infrastructure (user's explicit choice), gated behind `ALLOW_LIVE_BROADCAST_TEST=true`. Found and fixed four real bugs building it: Pest's browser plugin never actually navigates externally even given an absolute URL (always renders through its own in-process kernel); Reverb's own SEC-06 origin allowlist correctly rejected Pest's local origin (now also permits `127.0.0.1`, a deliberate widening); broadcasts aren't replayed to late channel subscribers; and — the deepest one — a child trigger process silently inherited phpunit.xml's `BROADCAST_CONNECTION=null`/`CACHE_STORE=array` via `getenv()`, so every attempt except the first manual dry-run broadcast nothing at all despite the DB writes succeeding. Full detail in [docs/testing.md](docs/testing.md) and [docs/decisions.md](docs/decisions.md).

**Earlier (2026-07-09)**: worked through the rest of TEST-01's remaining plan in one pass. Real browser coverage now exists (`tests/Browser/`, Pest's browser plugin + Playwright/Chromium) — a homepage load and the podium/lap-detail modal's real click-driven open/close, console-error-free, at desktop and mobile widths; deliberately doesn't test Escape/focus-trap since neither exists yet (A11Y-01). The MySQL two-connection map-variant-cap concurrency test found and fixed a **real race condition** — `ProcessNewLap::resolveMapVariant()`'s cap check had no locking at all, and two concurrent requests could push a map's variant count past its configured cap; fixed with `lockForUpdate()` on the base map row inside a transaction, same "lock what concurrent writers actually contend on" idea as the existing checkpoint-count CAS. Semgrep's CLI turned out to already be installed here — ran the real custom ruleset for the first time and found **two of the four rules were genuinely broken** (`hardcoded-credential-like-string` matched almost any string assignment; `blade-unescaped-output` never matched anything at all) — both fixed and all four now validated with real positive/negative fixtures (`tests/semgrep/`). PHPStan raised 6 → 7 (clean); level 8 was tried and deliberately not kept — 46 errors, real null-safety gaps needing actual code changes, not a one-pass job. **The level 6→7 pass itself caught a live example of exactly that risk**: a background agent's mechanical type-fix introduced a real regression in `Home::computeHighlights()` (blanket-wrapping every highlight's data in `array_values()`, which would have silently broken the Live Stats Snapshot block's string-keyed data) — caught and fixed before it shipped by checking the Blade partial's actual usage, not just trusting the type checker. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/security.md](docs/security.md).

**Also this session**: investigated a real Any Order lap the user got rejected live (checkpoint IDs `[1, 4, 5]`) — first instinct was that the SEC-04 contiguous-`1..N` checkpoint check was wrong for Any Order/Rally races and shipped a fix loosening it, but that was itself wrong and got caught before commit. The user correctly pointed out Any Order still requires hitting every checkpoint, just not in course order; real data confirmed it — `bloodgulch`'s established `checkpoint_count` is `5`, but the payload only had 3 splits, pointing to the game server's Lua script under-reporting splits in Any Order mode (a producer bug), not a backend gap. Reverted the loosening; the original check was correct all along (it already tolerates out-of-order submission via `sort()`, which is all "any order" actually changes). Read the real `hrl.lua` afterward and confirmed the actual mechanism: `NormalizeCheckpointId()`'s bitmask decoding only works for contiguous-low-bit patterns, which Any Order mode doesn't produce. `LapSubmissionTest.php` keeps the real anonymised payload as a permanent regression test asserting the correct rejection, plus a new test proving a genuinely complete-but-shuffled Any Order submission is accepted. Also clarified: Rally isn't currently usable at all due to an unrelated known Lua bug ("last lap doesn't count") — a real Rally golden fixture is out of scope until that's fixed game-side, removed from the active TEST-01 list. Full detail in [docs/decisions.md](docs/decisions.md).

**Earlier (2026-07-09)**: three more TEST-01 items closed. The read-API rate limiter's `60`/minute ceiling moved from a hardcoded value in `AppServiceProvider` to `config('api.rate_limit_per_minute')`, with a new test in `ApiTest.php` that asserts the production value directly and drives 60 real requests through before confirming the 61st `429`s (the earlier test only proved the middleware wiring under a substituted small ceiling). `QueryServer` (the real UDP client) now has genuine socket-level coverage in `tests/Unit/QueryServerTest.php` — a forked loopback fixture server, not just the `GameServerQuery` fake — covering a valid reply, reordered keys, two malformed-reply shapes, a real timeout, and pre-flight validation. PHPStan raised from level 5 to 6 and all 69 resulting errors fixed as a pure type-annotation pass (Livewire property/method array shapes, `render(): View` return types, a `LengthAwarePaginator` generic, `collect()` template-resolution fixes) — zero runtime behavior changed, and the current full suite is 200 tests / 611 assertions green. Also: found and fixed a second, worse bug in a different concurrent session's zero-servers fix for `ServerList` — it replaced the original "Undefined array key" 500 with a hard `TypeError` (assigning `false` to a strictly `array`-typed property); properly retyped `$featured` to `?array` end-to-end, with a permanent regression test. Remaining TEST-01 work: Lua producer fixes and later real successful Any Order/Rally golden fixtures, MySQL concurrent map-variant-cap test, browser/Echo/Reverb coverage, coverage/mutation evidence, hosted Semgrep custom-rule validation, and PHPStan level 8.

**Earlier (2026-07-09)**: TEST-01's cheap focused gaps were addressed. `PlayerShowTest.php` covers global-only favourite-server/display logic. An extreme-page Livewire test exposed and fixed the shared ranked-pagination integer overflow. Semgrep now scans the connected GitHub repository; its first `.npmrc` finding was addressed, while hosted enforcement of the custom `.semgrep.yml` rules still needs confirmation. The user explicitly deferred general CI while this is a solo-maintained project; reconsider it only if collaboration/release risk changes.

**Earlier (2026-07-08)**: PERF-03 closed — full result computation before pagination. The audit's recommendation was deliberately open-ended (add pagination, "define scale targets," move computation to SQL/snapshots "when thresholds are reached"), so both judgment calls were put to the user rather than picked solo: paginate the API now (yes), and define concrete scale-target numbers now without building the SQL/snapshot rewrite itself (yes). Shipped: `/api/v1/maps/{map}/leaderboard` now paginates via `?page=`/`?per_page=` (default 50, capped 100) using the same in-memory `LengthAwarePaginator` approach the Livewire leaderboards already use (`HasRankedLeaderboardPagination`) — `GlobalRanking::mapLeaderboard()` still ranks the whole map before a page is sliced out, so this bounds response size, not computation cost. Shipped under `v1` rather than bumping to `v2` (accepted deliberately — no known real consumers of this brand-new endpoint yet). Defined, not built: concrete revisit triggers grounded in real measurements (`GlobalRanking::scores()` ~200ms today for 691 ranked players → revisit past ~750ms or ~25k `lap_times` rows; per-map leaderboard ~21ms today for the biggest real map (188 players) → revisit past ~150ms or ~1,000 distinct players per map; Players List ~0.4s today → revisit past ~1s). New pagination tests in `ApiTest.php`. Full detail in [docs/performance.md](docs/performance.md) and [docs/api.md](docs/api.md).

**Earlier (2026-07-08)**: PERF-02 (missing indexes) fully closed, including the identity-constraint half that first looked blocked. Read every real query against `lap_times`/`players`/`servers`/`maps` across the codebase, then ran `EXPLAIN` against the real dev MySQL DB (not SQLite) for each hot pattern before writing anything — the audit explicitly asked for evidence, not guesses. Shipped two composite indexes with confirmed before/after `EXPLAIN` improvement: `lap_times(server_id, map_id, player_id, time)` (ProcessNewLap's best-time lookup went from an `index_merge` to `"Select tables optimized away"`) and `lap_times(server_id, created_at)` (Latest Laps/MostActiveServer queries lost their filesort). Two other candidate indexes were tried and dropped after `EXPLAIN`/`FORCE INDEX` showed MySQL's optimizer bypasses them anyway — `whereHas('server')`'s semijoin conversion defeats any `lap_times` index for the map/server leaderboard ORDER BY, and a real fix needs a five-call-site query rewrite, not an index; logged in [docs/roadmap.md](docs/roadmap.md), not shipped as dead weight. Applied to the real DB via `php artisan migrate --path=...`, not a blanket `migrate` — this DB's `migrations` table still carries the *original* app's migration filenames, so a full `migrate` collides trying to re-run this rebuild's from-scratch `create_*_table` migrations under different names; every migration here has always been applied this same targeted way.

The audit's "unique identity constraints after cleaning duplicates" half initially looked like a much bigger, riskier problem than expected: 820 `players` rows for only 246 distinct `hash` values (up to 137 sharing one), which read as either a huge pre-existing duplication bug or an unmitigated `firstOrCreate` race identical to two already-fixed ones (`servers`, `maps`) — raised to the user rather than merging solo. **The user corrected the premise**: `hash` alone has never been a reliable per-player identity (the game client stopped manufacturing one distinct hash per player upstream), and the real identity key is `(hash, name)` together — confirmed against real data as already unique with zero collisions, so the fix needed no merge decision at all. Shipped `players.unique(hash, name)`, updated `Player::firstOrCreate()` to match both columns, and added `violatesPlayerIdentityUniqueness()` to `ProcessNewLap`'s retry-on-collision handling, mirroring `servers`/`maps` exactly. Caught a second real bug while testing this: `violatesMapNameUniqueness()`'s SQLite fallback (`in_array('name', $e->columns)`) started false-matching the new `players` constraint (which also has a `name` column) since SQLite never populates `$index` — tightened to an exact column-set match. 174 → 178 tests, same pre-existing 21 failures/1 error unchanged. Full detail in [docs/performance.md](docs/performance.md) and [docs/security.md](docs/security.md).

**Earlier (2026-07-08)**: SEC-07 (least privilege) partially closed. `.env` was mode `0644`, world-readable by any local account that could traverse the project path — fixed with `chmod 0600` (it's owned solely by the app's own user/group, confirmed the app still boots fine after). The other half — the DB user holding `ALL PRIVILEGES` instead of just CRUD — was **explicitly deferred, not fixed**: the current user has no `GRANT OPTION` so it can't reduce its own privileges, closing it needs a MySQL admin connection this environment doesn't have (and shouldn't go looking for), and after discussing creating a separate least-privilege runtime user (`SELECT`/`INSERT`/`UPDATE`/`DELETE` only — this app's session/queue/cache all run on the `database` driver, so runtime never needs DDL), the user chose to skip it for now. `redesign_hrl` still has `ALL PRIVILEGES`. Full detail in [docs/security.md](docs/security.md).

**Earlier (2026-07-08)**: SEC-06 (Reverb origin/resource policy) closed and verified live. `config/reverb.php`'s app entry now restricts `allowed_origins` to `['redesign.hrl.effakt.info']` (installed Reverb matches origin against hostname only, no scheme), sets `max_connections` to a generous round `500` (not a measured figure — a backstop pending real traffic data), and enables the already-configured `rate_limiting` (60/60s, `terminate_on_limit` true). The recommendation mixed one concrete fix with two open judgment calls (the exact connection ceiling; edge-level per-IP limiting in Nginx Proxy Manager, infrastructure outside this repo) — put to the user via `AskUserQuestion` rather than picked solo. New `ReverbConfigTest.php`. This environment has no `sudo`/supervisor-socket access to restart the long-running `redesign-hrl-reverb` process itself, so the user restarted it; then verified live over the real Pusher WebSocket protocol with raw sockets (a plain HTTP-upgrade check is a false negative here — Reverb accepts the `101` upgrade for any origin and only rejects at the Pusher protocol layer afterward): allowed origin gets `connection_established`, a spoofed origin gets `pusher:error` code `4009`, and a 61st message on one connection is rejected and the connection terminated (matching `max_attempts: 60`/`terminate_on_limit: true`). Full detail in [docs/security.md](docs/security.md).

**Earlier (2026-07-08)**: `race_type` now gets its own `Map` identity — a direct reversal of the "label-only" design from the SEC-04 work below, decided by the user immediately after that work shipped ("yes, race_type should be its own map identity"). Scanned the blast radius before touching anything: `race_type` was only ever used inside `ProcessNewLap::mapLabel()` at submission time, and `MapList`/`MapLeaderboard`/`ServerMapLeaderboard` already work generically per `Map` row (the exact path the checkpoint-count fork already exercised) — so only `ProcessNewLap` needed changes. New `raceTypeMapName()` helper suffixes the base map name (`-anyorder`/`-rally`, kept deliberately separate from the human-readable label suffix so the two naming schemes — and the existing `-splits-{count}` checkpoint-fork pattern — can never collide), folded in before any of the checkpoint-count logic runs so the two forks compose cleanly (`bloodgulch-anyorder-splits-6`). Index 0 (normal races, the vast majority of real traffic) is a no-op suffix, so every existing real map row keeps its exact current `name` — no data migration needed. One accepted, pre-existing-pattern limitation, not a new bug: `RefreshLiveServerInfo`'s live "current map" detection can only ever resolve to the race_type-0 row, since the UDP query protocol has no `race_type` field — same limitation the checkpoint-count fork's own variants already have. 168 → 173 tests, same pre-existing 21 failures/1 error unchanged. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/security.md](docs/security.md).

**Earlier (2026-07-08)**: a second security review of SEC-04 found real gaps and got them closed. `splits.*.duration` now requires `gt:0` (previously unbounded below zero), and `StoreLapTimeRequest::withValidator()` rejects checkpoint IDs that are merely N *distinct* values (`[-7, 40, 999]` used to pass) rather than the map's real contiguous `1..N` sequence. `ProcessNewLap::resolveMap()`'s checkpoint-count baseline establishment is now a concurrency-safe conditional `UPDATE ... WHERE checkpoint_count IS NULL` instead of a plain read-then-write, backed by a real `unique(name)` index on `maps` (with a matching `violatesMapNameUniqueness()` retry helper, same pattern as the existing `servers` identity race). Adding that constraint surfaced a real pre-existing duplicate in the live dev/prod DB (`bloodgulch` ids 1/10, traced to dead legacy code) — merged via a new migration rather than left to block the constraint. `config('webhook.max_map_variants_per_name')` (default 3) now caps how many `{map_name}-splits-{count}` forks a mismatched checkpoint layout can create before being rejected outright (`422 checkpoint_layout_mismatch`), and all 9 real maps' `checkpoint_count` baselines were backfilled from their own historical splits instead of staying `null` until each map's next split-bearing submission. Two real design questions came up mid-fix and were put to the user rather than decided solo: keep-forking-with-a-cap vs. reject-outright (kept forking), and whether the pre-existing duplicate meant `race_type` should get its own `Map` identity (investigated and deferred — `race_type` was never persisted per-lap, so historical data can't be retroactively split by it; logged as a real future item in [docs/roadmap.md](docs/roadmap.md) instead of rushed in). 156 → 168 tests, same pre-existing 21 failures/1 error unchanged. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/security.md](docs/security.md).

**Earlier (2026-07-08)**: SEC-05 (missing response hardening headers) closed. New `App\Http\Middleware\AddSecurityHeaders` on both `web`/`api`: `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, a locked-down `Permissions-Policy`, `X-Powered-By` suppressed via `header_remove()`. The CSP portion needed real verification, not just plausible-looking directives — inspected the actual rendered homepage (no inline scripts, no external CDN deps) to write it, shipped as `Content-Security-Policy-Report-Only` specifically so it could be tested without risk, then drove real headless Chrome against the live domain (no browser-automation tooling was pre-installed, but `google-chrome` itself was) and grepped the console log for violations. Found exactly one: `'unsafe-eval'` needed by `script-src`, from Alpine.js's default `new Function()`-based expression evaluator — a static grep for that string in the built JS had found nothing (minified out), so only real execution caught it. Presented two fixes (weaken `script-src` vs. change how Alpine evaluates) rather than picking alone — **user chose the latter**. Turned out to need no dependency change: this app doesn't import `alpinejs` directly at all (it ships bundled inside Livewire's own JS asset), and Livewire already ships an official CSP-safe alternative build behind a first-party `csp_safe` config flag — enabled via one `config(['livewire.csp_safe' => true])` call. Re-verified the live site was actually serving the CSP build (byte-for-byte match, not assumed) and re-ran the same headless-Chrome check on both the homepage and a real Server Single page (the one page using the two most complex Alpine expressions in the app, found by grepping every Blade view first) — zero violations, zero JS errors, full content rendered both times. Added `SecurityHeadersTest.php` (4 tests). Still report-only, not enforced, until real traffic confirms it clean too. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/security.md](docs/security.md).

**Earlier (2026-07-08)**: SEC-04 (webhook resource-exhaustion inputs), checkpoint-count portion closed. Checked real data first: every map's checkpoint IDs form a stable, contiguous `1..N` set across every lap ever submitted for it (Bloodgulch always 1-5, Death Island always 1-14), so a map's layout is a learnable fact, not something needing a single arbitrary ceiling. Design agreed with the user (real judgment calls, not derivable from code): a flat protocol-wide ceiling (`config('webhook.max_checkpoints')`, default 20) plus a per-map learned baseline (new nullable `checkpoint_count` on `maps`, migrated against the real dev MySQL DB) set from the first split-bearing submission and enforced after that. A mismatch doesn't reject the lap or corrupt the original leaderboard — maps are only ever added, never redesigned in place, so it forks into its own `{map_name}-splits-{count}` `Map` row instead, a real distinct identity (stronger separation than `race_type` gets today, which only changes the display label on the same row). New `ProcessNewLap::resolveMap()` replaces the previous plain `Map::firstOrCreate()`; a splitless submission (~96% of real laps) never touches any of this. Caught and fixed a real gotcha before writing tests: `Map` needed an integer cast on `checkpoint_count`, confirmed directly in `tinker` against the real DB — without it, MySQL returns a string and the `===` comparison would silently never match, forking every submission. 5 new `LapSubmissionTest` cases added (learn/reuse/fork/splitless/ceiling).

**SEC-04 fully closed, same day**: the remaining numeric-range portion (`player_time`/duration/timestamp bounds). Queried real data for each field before picking thresholds: `player_time` 15.3s-1170.9s, split `duration` 1.17s-182.2s with zero real splits ever exceeding their own lap's `player_time` (a real invariant, confirmed via a joined query). `startTime`/`endTime` reversed the obvious assumption — real data has a large fraction of absolute-clock-style values (up to a literal `999999.99` sentinel in 34 rows) that aren't lap-relative, and neither field feeds any real leaderboard logic (only `duration` does), so tightening them would reject real submissions for no functional benefit. Implemented (confirmed with the user first): `player_time` gets `lte:config('webhook.max_lap_time_seconds')` (3600s default); `splits.*.duration` gets `lte:player_time` (Laravel's `lte` rule compares against another field directly, confirmed via a real `Validator::make()` call, no custom rule needed); `startTime`/`endTime` get only a loose `gte:0`/`lte:99999999` overflow guard. 6 more tests added. 150 → 156 tests, 335 → 351 assertions, same pre-existing 21 failures/1 error unchanged. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/security.md](docs/security.md).

**Earlier (2026-07-08)**: PERF-01 (Home page recomputation) fully closed, in three passes. Pass 1 measured and deduped: `\DB::listen()` confirmed **132 queries/~2.3s** at real scale; `fastestImprovements()`/`achievements()` each independently queried the same recent-laps window and both re-ran `GlobalRanking::scores()`'s full every-lap computation once per recent lap just to read one player's row. Fixed in `Home.php` by computing the shared recent-laps query and one "current" `scores()` snapshot once per request — 132 → 94 queries, ~2.3s → ~1.5s. Re-audited same day, correctly called "partially improved, not resolved" (DB time was only ~170ms of that remaining ~1.5s — every visitor between two lap submissions was still paying the same PHP-side cost independently). Pass 2: asked the user to pick caching scope (inside `GlobalRanking` app-wide vs. `Home` only, a real architectural decision) — **user chose Home-only** — then cached the entire `$highlights`/`$quickStats` output as one blob, invalidated by a new `App\Listeners\InvalidateHomeHighlightsCache` on the already-firing `LapSubmitted` event, with a 10-minute TTL as a safety net only. Pass 3 (code review, same day): the pass-2 caching itself had two real bugs — a cache **stampede** (`Cache::remember()` has no locking, so every visitor rebuilds at once right after invalidation) and a more serious **stale-write race** (a rebuild already in flight when a new lap invalidates the cache would finish afterward and silently write stale data back, undoing the invalidation for up to the full TTL) — plus a factual error in the listener's docblock (`ProcessNewLap` isn't queued at all; it deliberately runs synchronously in the webhook's own request). Fixed with a generation key (bumped on invalidation instead of deleting the data key directly, so a stale in-flight rebuild can only write to an abandoned key) plus a `Cache::lock()` with a cache recheck on acquisition. Caught two more real things along the way: `Cache::increment()` on the `database` cache store returns `false` and does nothing if the key was never set (needs an explicit `Cache::add(..., 0, null)` first); and Pest's `CACHE_STORE=array` persists across the whole test process, so one test's cached value could leak into another's assertions (fixed globally via `Cache::flush()` in `tests/Pest.php`'s `beforeEach()`). Result: cache hit = 1–2 queries/<1ms; live TTFB held at ~37–53ms throughout, clearing both the under-20-queries and under-500ms targets with large margin. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/performance.md](docs/performance.md).

**Earlier (2026-07-08)**: SEC-02's "Not verified" gap turned out to be real, then the first fix for it shipped a real open redirect, caught the same day by the user re-testing the live site. Round one: `curl -sSI http://redesign.hrl.effakt.info/` returned a plain `200 OK` instead of a redirect — root-caused (temporary debug route, added/curled/removed immediately) to FastPanel's nginx unconditionally setting `$_SERVER['HTTPS']="on"` regardless of the real connection, so `RedirectIfNotSecure` never fired. Fixed by trusting the (accurate) `X-Forwarded-Proto` header from Nginx Proxy Manager via `$middleware->trustProxies(at: '*', headers: FOR|HOST|PORT|PROTO)`. Round two: the user immediately found that trusting `HOST`/`PORT` too made this a live, working **open redirect** — `X-Forwarded-Host: evil.example` + `X-Forwarded-Port: 444` produced `Location: https://evil.example:444/...`. Fixed by dropping `HOST`/`PORT` from the trusted mask (kept `FOR`/`PROTO` only — the edge overwrites forged versions of those two but passes `HOST`/`PORT` through as-is). Also removed `AddHstsHeader` entirely (deleted the class) per explicit instruction: it's host-wide, so it was quietly threatening to force HTTPS on `/api/v1/*` too, which must stay plain-HTTP for legacy Halo game-server clients — this should have been the call when SEC-02 first shipped. Verified live after both rounds: spoofed-header requests now redirect to the real domain, the normal redirect and `/api/v1/*` plain-HTTP access are unaffected, and HSTS is no longer sent. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/security.md](docs/security.md)'s SEC-02 section.

**Earlier (2026-07-08)**: five real live-update bugs total, all found the same day the OPS-01 work above closed, across two rounds of live-testing by the user. Round one (three bugs, all in the same 9 listeners across `Home`, `ServerShow`, `ServerPlayerShow`, `PlayerShow`, `ServerList`, `MapList`, `PlayerList`, `ServerMapLeaderboard`, `MapLeaderboard`): missing leading dots on custom `broadcastAs()` event names; `echo-public:` isn't a real Livewire 4 channel-type prefix at all (confirmed by reading `vendor/livewire/livewire/dist/livewire.esm.js` — only `channel`/`private`/`encryptedPrivate`/`presence`/`notification` are recognized; `public` silently no-ops), fixed by switching all 9 to plain `echo:`; and three listeners (`ServerShow::loadServerData()`, `PlayerShow::loadProfile()`, `ServerPlayerShow::loadProfile()`) crashing on the real payload since they were typed for Eloquent models, fixed with parameterless `onLapSubmitted()` wrappers. Round two, found once round one's fixes let real events through for the first time: `MapLeaderboard`/`ServerMapLeaderboard`'s total-lap counts only updated on a PB (they listened only on `LeaderboardUpdated`'s scoped channels) — retargeted both to `LapSubmitted`'s site-wide `activity` channel, a strict superset trigger, renaming their listeners to `onLapSubmitted()` too; and the scheduled server-status poll (`RefreshLiveServerInfo`, every minute) never broadcast anything at all, so `ServerList` only picked up an online/current-map/player-count change via an unrelated lap submission elsewhere — fixed with a new `App\Events\ServerStatusRefreshed`, dispatched once per command run, and a second `#[On(...)]` attribute stacked onto `ServerList::loadServers()`. **Real test-coverage gap, still not closed**: the existing Pest suite calls listener methods directly rather than through a real Echo/Pusher event, so it passed through every one of these bugs — see [testing.md](testing.md). Full detail on both rounds in [docs/decisions.md](docs/decisions.md) and [docs/database.md](docs/database.md)'s "Live leaderboard updates" section.

**Earlier (2026-07-08)**: closed OPS-01 — Reverb, the queue worker, and the scheduler are now supervisor-managed (not bare/manually-started processes or a missing crontab entry), via conf files added to `../../supervisord/` (a sibling directory outside this Git root — see [docs/deployment.md](docs/deployment.md)) (`redesign-hrl-reverb.conf`, `redesign-hrl-schedule.conf` new; `redesign-hrl-laravel-worker.conf` pre-existing, had a broken path fixed). The scheduler runs via `schedule:work` rather than a crontab entry — this app's user has no crontab set up, and `schedule:work`'s internal loop achieves the same effect without needing one. Verified live: `schedule:work`'s log shows `RefreshLiveServerInfo` actually firing every minute, and a real WebSocket-upgrade `curl` against the supervisor-managed Reverb process returned `101 Switching Protocols`. Full detail in [docs/deployment.md](docs/deployment.md)'s "Process management (OPS-01)" section.

**Phase 1 (frontend design) is complete. Phase 2 (backend/real data) is now complete too** — every page is wired to real data: Servers List (`ServerList`), Server Single (`ServerShow`: Maps, Stats Card, Latest Laps, and Top Players/Server Score), the nested Server Map Leaderboard (`ServerMapLeaderboard`), Maps List (`MapList`), the global Map Leaderboard (`MapLeaderboard`, real pagination for ranks 4+ via `HasRankedLeaderboardPagination`), Players List (`PlayerList`, a real Global Leaderboard), Player Single (`PlayerShow`, real Global Rank/Score/Points and real split comparison), and Home (every highlight block is real: Quick Stats, Live Stats Snapshot, New Maps/Servers, Fastest Improvements, Player Achievements, Most Active Server, and Latest/Current Records — none left mock or empty-by-necessity). All ranked leaderboards share real gaps, server/player identity, and split comparison via `LapTimeSplit::compare()`. **A public, read-only, versioned API now exists too** (`/api/v1/servers`, `/api/v1/maps/{map}/leaderboard`, `/api/v1/laps/{lapTime}` — no auth, rate-limited, see [docs/api.md](docs/api.md)), **the lap-submission webhook is rebuilt and live** (`POST /api/v1/laps`, now with SEC-01 HRL query verification — see below), **and server cards now show genuinely live current-map/online status** (roadmap item 19) rather than only lap-history-derived proxies. **Real-time updates are wired up too** (roadmap item 16) — Reverb/Echo, live-updating the nested/global map leaderboards on a new PB, and Servers List's header stats/"MOST ACTIVE" card and every Home highlight on *any* logged attempt. **A server-scoped player profile now exists too** (roadmap item 20, see below) — `/servers/{serverId}/players/{playerId}`, reached from Server Single's Top Players list. Test suite: 141 Pest tests / 355 assertions (2026-07-07), PHPStan clean, Rector configured (fixes not yet applied). See [docs/roadmap.md](docs/roadmap.md) for the full plan, and [docs/database.md](docs/database.md) for real data quirks the wiring surfaced: `lap_times`' timestamp precision, duplicate pivot rows (root cause now confirmed and fixed at the write site), archived-server exclusion, and sparse split coverage (~4%).

**Just done (2026-07-07)**: a site audit (`SITE_AUDIT.md`) flagged the open lap-submission webhook as critical (SEC-01) — anyone could POST fabricated leaderboard data. Rather than the audit's suggested TLS/HMAC relay (real infrastructure outside this repo, on every game-server host), `App\Helpers\LapSubmissionVerifier` cross-checks every submission against a live UDP `\query` response from the submitting ip:port, reusing the protocol already built for item 19 — the Lua script publishes `hrl_enabled`/`hrl_protocol`/`hrl_token` via SAPP's existing `query_add`, and verification requires that plus a matching map and an online player name. **`config('webhook.hrl_query.enforce')` defaults to `false`** (log-only) since real game servers' Lua scripts don't send these fields yet — flip it once every active server's script is confirmed updated, or legitimate submissions will be rejected.

**Same-day follow-up**: a second review of that implementation (findings recorded directly in `SITE_AUDIT.md`) caught 5 real gaps, all fixed the same day: (1) the webhook rate limiter's `ip:port` key was bypassable by rotating the caller-supplied port — now two limits apply together, the existing per-`ip:port` one plus a coarser per-IP ceiling; (2) the idempotency guard reserved its cache key *before* processing, so a pre-commit failure left retries permanently stuck and a legitimate retry got a bare `409` instead of its original result — reworked into a real `processing`/`done` state machine that replays the original response, plus an optional client-supplied `submission_id`; (3) `LapSubmissionVerifier` now accepts an `hrl_token_prev` field for token-rotation grace; (4) removed a duplicate UDP round-trip — `ProcessNewLap` now reuses the verifier's already-fetched query response instead of querying the same ip:port again; (5) tightened online-player matching from "any `player_`-prefixed key" to `/^player_\d+$/`. 117 → 125 tests, 286 → 308 assertions. Full detail in [docs/security.md](docs/security.md) and [docs/decisions.md](docs/decisions.md).

**Second same-day follow-up**: a third review found 5 more gaps. Two involved genuinely open-ended design decisions (exact rate-limit numbers; how much idempotency infrastructure to build) — surfaced to the user via AskUserQuestion rather than decided solo, per a new standing rule (`feedback_audit_scope_discipline` in memory: ask before implementing anything an audit phrases open-endedly). Durable idempotency: a nullable `submission_id` column + unique `(server_id, submission_id)` index on `lap_times` (user's pick over a separate claims table) — `ProcessNewLap` catches the resulting `QueryException` on a genuine collision and replays a best-effort current-state response. Rate limiting: a two-tier system (user's pick over just lowering one number) — "unverified" sources get a strict allowance, "verified" sources (a request from that exact ip:port already passed HRL verification, cached 5 min) get a much more generous one; verification still runs every time regardless of tier. The other three findings had one unambiguous fix each, done directly: namespaced the idempotency key by ip:port even with a client-supplied `submission_id` (prevents two servers' similar counters colliding); split one shared 10s cache TTL into a 30s processing-reservation window and a 300s result-retention window; gave `ProcessNewLap`'s `$liveQueryResponse` a real tri-state so a totally-failed verification query doesn't trigger a third UDP attempt. 125 → 129 tests, 308 → 321 assertions. Full detail in [docs/security.md](docs/security.md) and [docs/decisions.md](docs/decisions.md).

**Third same-day follow-up**: a fourth review verified the staging deployment directly and found 4 more gaps. Two unambiguous, done directly: revoke the verified rate-limit marker immediately on any failed verification (was only expiring naturally after 5 min); a reused `submission_id` with genuinely different content now returns `409 idempotency_conflict` (stores a content hash alongside the cached response) and `submission_id` is `required` once enforcement is on. The other — no unique `(ip, port)` constraint on `servers`, so `Server::firstOrCreate()` itself wasn't race-proof — took three real attempts (recorded in the migration's docblock and [docs/decisions.md](docs/decisions.md)): a plain `unique(ip, port)` blocks reusing an archived server's ip:port forever; `unique(ip, port, deleted_at)` silently never fires because SQL treats `NULL != NULL`; a generated column referencing the row's own `id` is flatly rejected by MySQL — **which is this app's real dev/prod database, not just the SQLite the Pest suite uses** (a fact now saved to memory, discovered the hard way here). Shipped: a generated `active_since` column collapsing every active row to one sentinel value while soft-deleted rows keep their real `deleted_at`, so active duplicates still collide but archived ones and legitimate reuse don't; `ProcessNewLap` now retries the whole transaction once on a `UniqueConstraintViolationException` its `violatesSubmissionIdUniqueness()` helper doesn't attribute to the lap-level constraint (replacing an earlier, over-broad generic-SQLSTATE check). Given the constraint's complexity grew well past "just add a unique index," this was also confirmed with the user before building, per the same standing ask-first rule. 129 → 133 tests, 321 → 331 assertions. Full detail in [docs/security.md](docs/security.md) and [docs/decisions.md](docs/decisions.md).

**Fourth same-day follow-up**: a fifth review inspected the deployed MySQL schema directly (confirmed the generated `active_since` column, its unique index, no duplicate active identities, archived-address reuse working) and found 3 more gaps, all narrow enough to fix directly without going back to the user. The `submission_id` content-hash conflict check (third follow-up) only worked while its cache entry was alive — reused after cache expiry/eviction/restart, it silently replayed the old lap. Fixed by persisting the fingerprint: a nullable `submission_hash` column on `lap_times`, compared during durable replay and turned into the same `409 idempotency_conflict` via a new `LapSubmissionConflictException`. The hash itself was also missing `race_type`/`player_name` and hashed splits positionally instead of by `checkpoint_id` — extracted into a shared `App\Helpers\LapSubmissionHash::compute()` used by both the controller and the job, so they can't disagree on what counts as "the same lap." Lastly, `ProcessNewLap` assumed by elimination that any non-`submission_id` unique violation was the `servers` race — now checks the exception's `$columns`/`$index` for that constraint by name (`violatesServerIdentityUniqueness()`) and rethrows anything else. 133 → 137 tests, 331 → 349 assertions. Full detail in [docs/security.md](docs/security.md) and [docs/decisions.md](docs/decisions.md).

**Just done (2026-07-07)**: real-time updates now reach every page, not just the four originally wired (the two leaderboards, Servers List, Home). `ServerShow`, `ServerPlayerShow`, `PlayerShow`, `PlayerList`, and `MapList` had zero live-update listeners — confirmed the gap by watching Server Single live in a browser during a real webhook submission and seeing nothing move. Each now listens on the same site-wide `activity` channel (`#[On('echo-public:activity,lap.submitted')]`), re-running its own `mount()` logic (extracted into `load*()` methods) on receipt — see [docs/database.md](docs/database.md)'s "Every remaining page wired up" note.

Earlier the same day: REL-01 (real-time updates not reaching public visitors at all) fully resolved, three root causes found and fixed. (1) `VITE_REVERB_HOST`/`PORT`/`SCHEME` had been aliased to the server-side `REVERB_*` vars — correct for Laravel-to-Reverb loopback publishing (`localhost:8081`), wrong for the public JS bundle a real browser loads; decoupled them (`VITE_REVERB_HOST` set to the real public hostname, port `443`, scheme `https`) and rebuilt. (2) The Reverb server process simply wasn't running for this app at all (nothing listening on 8081); started it — not yet durable (no systemd/supervisor unit, see OPS-01, still open). (3) No nginx reverse-proxy existed for the WebSocket upgrade — the real topology turned out to be **Nginx Proxy Manager → FastPanel's nginx → PHP-FPM**, not FastPanel directly facing the internet as first assumed; the user added an NPM Custom Location forwarding `/app` to this container's internal LAN IP on port 8081, with WebSocket support enabled. Verified with a real `101 Switching Protocols` handshake (both the user's check and independently from this environment).

Earlier the same day: fixed SEC-02 (HTTP served without a redirect/HSTS) while keeping `/api/v1/*` on plain HTTP for legacy Halo game-server clients that can't do TLS — scoped `App\Http\Middleware\RedirectIfNotSecure`/`AddHstsHeader` to the `web` middleware group only in `bootstrap/app.php`, leaving the `api` group untouched, plus `SESSION_SECURE_COOKIE=true`. Depends on nginx setting `fastcgi_param HTTPS on` on the TLS vhost (not verified — no read access to the FastPanel-managed config here); see [docs/security.md](docs/security.md) and [docs/decisions.md](docs/decisions.md).

Earlier the same day: user flagged that the rebuild dropped a legacy behavior — `ApiController.php-legacy` hardcoded a rewrite of three known internal/NAT addresses (a UniFi router at the old hosting site sometimes resolved the submitting game server's IP as its own internal address instead of the real public one) to the real public IP before processing. Ported as a config-driven equivalent rather than a hardcoded snippet: `config('webhook.internal_ip_map')` plus a new `App\Helpers\ResolveSubmittingIp::resolve()`, applied in both `LapSubmissionController::store()` (before the ip is used for the idempotency key, verification, or storage) and the `webhook` rate limiter in `AppServiceProvider` (which runs as middleware, before the controller, and must resolve the same ip or the "verified" tier's cache marker would never match). 139 → 141 tests, 352 → 355 assertions.

**Fifth same-day follow-up**: a sixth review confirmed the deployed `submission_hash` column and found one narrow remaining gap plus a doc drift. `LapSubmissionHash::compute()` hashed validated fields as-is, but Laravel's `numeric`/`integer` rules accept a numeric string without coercing it — so `player_time`/`checkpoint_id` arriving as `42.5` vs `"42.5"` could hash differently for the same submission. Fixed by casting every numeric field to a canonical type before hashing. Also added `distinct` to `splits.*.checkpoint_id` so two splits can't claim the same checkpoint (previously left the sort-by-checkpoint normalization payload-order-dependent for that case), and corrected a migration docblock that said laps without a `submission_id` have no fingerprint — every new lap gets one now. 137 → 139 tests, 349 → 352 assertions. Full detail in [docs/security.md](docs/security.md) and [docs/decisions.md](docs/decisions.md).

**Just done (2026-07-06)**: shipped a server-scoped player profile (roadmap item 20), on request — clicking a player from Server Single's Top Players list previously landed on the global Player Single page, losing server context. New `/servers/{serverId}/players/{playerId}` (`App\Livewire\Servers\ServerPlayerShow`) shows Server Rank/Server Score alongside Global Rank/Global Score for context, every stat isolated to that server, plus a link to the full global profile. Extracted `App\Models\PlayerProfile::build()` from `PlayerShow::mount()` — the second real consumer of the same "assemble a player's per-map/recent-lap/stats breakdown" logic — parameterized by an optional `$serverId`. Caught a real regression from the extraction itself: `PlayerShow`'s original inline code tolerated `GlobalRanking::forPlayer()` returning `null` (PHP's forgiving `null['key'] ?? default`), but the extracted method's tightened `array $ranking` signature turned that into a real `TypeError` — caught immediately by the existing `RoutesTest.php` smoke suite, fixed by widening to `?array`. Added `ServerPlayerShowTest.php` (4 tests) and extended `ServerShowTest.php`/`RoutesTest.php`. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/player-single.md](docs/player-single.md).

Earlier the same day: wired up Reverb/Echo for real-time leaderboard updates (roadmap item 16). `App\Events\LeaderboardUpdated` (firing since item 14, previously going nowhere) now broadcasts on two public channels — `servers.{serverId}.maps.{mapId}` and a new map-only `maps.{mapId}` — and `ServerMapLeaderboard`/`MapLeaderboard` both listen via Livewire's `#[On('echo-public:...')]` attribute, re-fetching their ranking on receipt (simpler and more obviously correct than patching one new lap into an already-ranked array). Verified end-to-end with a standalone `pusher-js` script that bypasses Echo/Livewire entirely, connecting directly to the local Reverb server and confirming the real WebSocket payload arrives on a real PB submission — this also surfaced two genuine operational requirements now documented in [deployment.md](docs/deployment.md): broadcasting is queued (needs a running queue worker, not just Reverb — a submitted PB silently went nowhere until one was started) and this dev environment's default Reverb port (8080) was already taken by something else on the shared host (moved to 8081). `ServerMapLeaderboard` had zero test coverage before this — added `ServerMapLeaderboardTest.php` alongside the item 16 test coverage.

**Immediate follow-up, same day**: user asked whether the "Most Active" card updates live, then — once confirmed it didn't (real data, but page-load-only) — asked to extend live updates "everywhere it's visible." New `App\Events\LapSubmitted` fires on *every* logged attempt (not just a PB) on a site-wide `activity` channel, since Servers List's header stats/"MOST ACTIVE" Activity Score and Home's highlights change on any attempt anywhere, not one map. `ServerList`/`Home`'s `mount()` bodies became `loadServers()`/`loadHighlights()`, reused by a matching `#[On('echo-public:activity,lap.submitted')]` listener each. Verified the same way (standalone `pusher-js` script + a real webhook submission). Also: made a real mistake mid-task — ran `git checkout --` on a file with the user's own uncommitted in-progress edit (`PLAYERS_PER_PAGE` 15→10) while isolating an unrelated test failure, silently discarding it; caught immediately and restored by hand. See [docs/decisions.md](docs/decisions.md) for the full incident note. Full detail in [docs/decisions.md](docs/decisions.md).

Earlier the same day: shipped live server info (roadmap item 19). Queried the actual production server (id 7 — genuinely live and reachable) to empirically confirm the `QueryServer` protocol's real response fields rather than guessing: `mapname` matches `maps.name`'s format exactly, `numplayers` gives a live player count, and the `sapp` field confirms this is a Halo PC server running the SAPP mod. New scheduled command `App\Console\Commands\RefreshLiveServerInfo` (every minute) stores the result on new `servers` columns (`current_map_id`, `live_player_count`, `queried_at`, `query_successful`) via a real migration run against the dev DB. `ServerList` prefers fresh (≤5 min) live data for "online"/"now playing," treats a fresh *failed* query as authoritative for "offline" specifically, and falls back to the pre-existing lap-history proxies once data goes stale — a failed query never wipes previously-known good values. Never fabricates a `Map` row from an unrecognized live `mapname` (same principle as the webhook). Also fixed a real, pre-existing Larastan gap along the way: it doesn't resolve a custom `casts(): array` Carbon cast for non-standard column names in this project — fixed with `@property` docblock annotations on `Server` (the standard mechanism, not a suppression).

**Immediate follow-up fix, same day**: user feedback caught that "online" was misleading — a reachable-but-empty server showed as online, when a leaderboard's whole point is "is there a race happening." Refined rather than blanket-changed: the **table**'s "online" (and the "N ONLINE" header count) keeps meaning "the process is reachable," but the **featured card**'s "ONLINE" badge alone now additionally requires a live player count > 0. Verified against real data: server 7, reachable with 0 players, shows as online in the table but the featured card correctly hides its "ONLINE" badge. Added `RefreshLiveServerInfoTest.php` (4 tests) and extended `ServerListTest.php` (+5, across both changes). 79→88 tests, 208→233 assertions. Full detail in [docs/decisions.md](docs/decisions.md) and [docs/database.md](docs/database.md).

Earlier the same day: rebuilt the lap-submission webhook end-to-end (roadmap item 14) — `POST /api/v1/laps` → `LapSubmissionController` → `ProcessNewLap`. Recovered the previously-missing `app/Helpers/QueryServer.php-legacy` helper, confirming the UDP `QueryServer` protocol (a GameSpy-style `\query` request) that items 14 and 19 were both blocked on; rebuilt it with real key/value parsing instead of the legacy's fragile fixed-array-offset reads. Deliberate behavioral changes beyond the old app, all explicit decisions not silent ports: logs every lap attempt (not just PB improvements), a failed live query no longer discards the lap, the duplicate-pivot-row bug is fixed at the write site (`syncWithoutDetaching`), and a new `LeaderboardUpdated` broadcast event exists (ready for item 16, not yet consumed). **No auth** — surfaced to the user explicitly before building; decided to keep it open, matching the old app. Also caught and fixed a real SQLite/PDO bug along the way: a bound parameter compared against an aggregate in `WHERE`/`HAVING` is silently ignored by this environment's bundled SQLite 3.34.1 (confirmed via raw PDO outside Laravel) — fixed by computing the leaderboard-position comparison in PHP instead of SQL. Added `LapSubmissionTest.php` (9 tests). Full detail in [docs/decisions.md](docs/decisions.md), endpoint documented in [docs/api.md](docs/api.md).

Earlier the same day: built the new read API (roadmap item 15) — three public, read-only endpoints under `/api/v1` (`servers`, `maps/{map}/leaderboard` with `?server=` for the nested scope, `laps/{lapTime}`), no auth (the site's already fully public, nothing new exposed), rate-limited (60/min/IP). New `GlobalRanking::mapLeaderboard()` calculator method backs the leaderboard endpoint — the third occurrence of "rank one map's best laps," so it got its own canonical source rather than a fourth ad-hoc copy. Caught a real bug via testing the "still works if archived" case explicitly: the lap-detail endpoint threw a 500 for any lap whose server was later soft-deleted, since `LapTime::server()` deliberately excludes trashed servers everywhere else in this app — fixed by loading the server with `withTrashed()` for just this one endpoint. Added `ApiTest.php` (10 tests). Full detail in [docs/decisions.md](docs/decisions.md), endpoint shapes in [docs/api.md](docs/api.md).

Earlier the same day: real Pest coverage for all four Phase 2 calculators (item 17, partial) — `GlobalRankingTest.php`, `MostActiveServerTest.php`, `RecordHistoryTest.php`, `HomeTest.php`, plus pinning `GLOBAL_RANKING_VARIANT=sum` in `phpunit.xml` so tests never silently inherit this dev environment's `.env` (still set to `average` for manual comparison). Before that: the historical record-breaking-events derivation (item 13, new `App\Models\RecordHistory`, **every Homepage highlight is now real**), Most Active Server scoring (item 12), `Home` wired to real data end-to-end (item 8, caught a real sandbagging exploit), and the full Global Player Ranking implementation (item 11). All of the above in [docs/decisions.md](docs/decisions.md) (chronological, most recent at the bottom).

Earlier the same day: `Home` was wired to real data end-to-end (item 8 done), which surfaced and fixed a real exploit — a player could sandbag a deliberately bad first lap then a merely-average one to fake a huge "biggest improvement" highlight despite landing at a non-competitive rank; fixed by requiring the resulting rank to actually earn points. Also fixed the two Fastest Improvements bullets repeating the same event, and found (but didn't act on) a real data-quality fact: 8 different `Player` rows are all named "Nuevo001" (distinct hashes, genuinely different accounts — see [docs/roadmap.md](docs/roadmap.md)'s open questions). And before that: the full Global Player Ranking implementation (item 11) — the `GlobalRanking` calculator, a real ranked table on Server Single, unified Records/Maps/Laps stats, and an A/B-able `sum` vs. `average` score variant (`config('ranking.global_score_variant')`, env `GLOBAL_RANKING_VARIANT`) with a regularized (Bayesian) average. **This dev environment's `.env` currently has `GLOBAL_RANKING_VARIANT=average` set deliberately** — check `.env` before assuming `sum`'s behavior. All of the above in [docs/decisions.md](docs/decisions.md) (chronological, most recent at the bottom).

Next real blocker-free work: item 17 (remaining Pest coverage for non-algorithmic Livewire display logic) — see [docs/roadmap.md](docs/roadmap.md). Every other roadmap item is now done.

## Where to find things

| Topic | Doc |
|---|---|
| What's in/out of scope, non-goals | [docs/scope.md](docs/scope.md) |
| Stack, directory layout, routing hierarchy, styleguide | [docs/architecture.md](docs/architecture.md) |
| Real schema, data model decisions, webhook plan | [docs/database.md](docs/database.md) |
| Global Player Ranking spec (planned, not built) | [docs/global-ranking.md](docs/global-ranking.md) |
| Most Active Server scoring spec (planned, not built) | [docs/most-active-server.md](docs/most-active-server.md) |
| Server Single page additions spec (built, mock data) | [docs/server-single.md](docs/server-single.md) |
| Players List → Global Leaderboard redesign spec (built, mock data) | [docs/players-list.md](docs/players-list.md) |
| Player Single page expansion spec (built, mock data) | [docs/player-single.md](docs/player-single.md) |
| Homepage redesign spec (built, mock data) | [docs/homepage.md](docs/homepage.md) |
| Secrets handling, auth status, known issues | [docs/security.md](docs/security.md) |
| Project-specific conventions (Livewire, Tailwind, Blade, mock data) | [docs/coding-standards.md](docs/coding-standards.md) |
| Planned REST endpoints | [docs/api.md](docs/api.md) |
| Livewire + Alpine patterns and gotchas (read before touching any component) | [docs/livewire-guide.md](docs/livewire-guide.md) |
| Scale, query strategy, real-time updates | [docs/performance.md](docs/performance.md) |
| Test status and plan | [docs/testing.md](docs/testing.md) |
| Environment, build process, cutover plan | [docs/deployment.md](docs/deployment.md) |
| **Chronological decision log** — the "why," including reversals and incidents | [docs/decisions.md](docs/decisions.md) |
| What's done, what's next, open questions | [docs/roadmap.md](docs/roadmap.md) |
| Domain terms (Player vs. Driver, nested vs. global leaderboard, split vs. sector, etc.) | [docs/glossary.md](docs/glossary.md) |

## Ground rules

- Nothing in this app is a like-for-like port of the old site by default — where the redesign's UX diverges (e.g. the server-scoped vs. global leaderboard split), the new design wins. Check [docs/decisions.md](docs/decisions.md) before assuming old-site behavior is the target.
- The whole frontend currently runs on mock data by deliberate sequencing choice, not oversight. Don't "fix" this unprompted — wiring real data is a planned, sequenced step (see [docs/roadmap.md](docs/roadmap.md)).
- Read [docs/livewire-guide.md](docs/livewire-guide.md) before writing or editing any Livewire component — several non-obvious framework gotchas are already solved there; don't rediscover them.
- Follow [docs/coding-standards.md](docs/coding-standards.md) for project-specific conventions not covered by the general Laravel Boost guidelines (see `CLAUDE.md`).
