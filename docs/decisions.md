# Decisions

Chronological record of architecture decisions, reversals, and incidents — the "why" behind choices in the other docs. Newest at the bottom of each section's history isn't tracked; entries are grouped by topic instead.

## Fresh reinstall, not incremental upgrade

Old app: Laravel 6.2, PHP 7.2, both EOL. Decided against incremental upgrade — fresh install on Laravel 13.8 (planned as "12" originally), keeping the existing database. Migrations are additive/conditional against the real schema, not naive fresh `create` migrations.

## Full lap history, not personal-best-only

**Originally planned** (before the real schema was inspected): a `lap_records` table upserted on personal best (old value overwritten), plus a separate lightweight `lap_events` table for activity tracking, plus `lap_record_splits` keyed by `sector_number`.

**Reality after inspecting the schema**: none of those tables exist. The real `lap_times` table is already append-only full history (every lap ever submitted), and `lap_time_splits` keys by `checkpoint_id` with richer `start_time`/`end_time`/`duration` fields.

**Decision**: keep full history as the real schema already does. Personal bests and course records become derived `MIN(time)` queries, not stored rows. This also means `lap_times`' own timestamp doubles as the activity/recency signal — no separate `lap_events` table needed. See [database.md](database.md).

## Claim-code ownership system: deferred

`users_players`/`users_servers` (claim-code based ownership of player/server profiles) exist in the real schema and are a genuine old-app feature, not legacy cruft. Decision: preserve the tables as-is, but no new feature work is planned around them until explicitly revisited. Don't design new features assuming ownership/claiming is wired up.

## Server-scoped vs. global leaderboards

**Originally built**: a single flat `/maps/{mapId}` route, reached directly from the Servers list, showing one leaderboard.

**User clarification**: the intended hierarchy has two distinct concepts —
- A **nested, server-scoped leaderboard**: Servers → a server's own map list → that server+map's leaderboard.
- A **global leaderboard**: a top-level Maps list (like Players) → a map's leaderboard aggregated across all servers.

**Decision**: split into `Servers\ServerMapLeaderboard` (nested) and `Maps\MapLeaderboard` (global), each reached via its own route hierarchy (`/servers/{id}/maps/{id}` vs. `/maps/{id}`). This is a deliberate addition beyond the old site, which never had a per-server map list route (`/servers/{id}` 404s there) — new UX for the redesign, not a parity port. The two components share their ranked-table/modal logic via `HasLapDetailModal` + Blade partials rather than duplicating ~150 lines twice.

## Variable split/sector count (up to 14, not always 3)

Some maps have up to 14 splits/sectors, not the fixed 3 the initial design assumed.

**Decided**:
- **Podium card**: replaced the 3-box split-time grid with a compact multi-segment sparkline bar (width per segment proportional to split time, green/gold color-coded vs. the lap's own average) — scales to any split count without adding rows. Individual split times moved off the podium entirely; a hover tooltip shows them instead.
- **Lap Detail modal**: capped panel height (`max-h-[85vh]`), made the split-rows area independently scrollable with a sticky column header, keeping the lap-time header and final-delta/buttons footer pinned.
- Mock data updated from 3 to 14 entries specifically to exercise this, not just build-to-spec unverified.

## Incident: fatal error on every Livewire click (missing `cache` table)

`.env` has `CACHE_STORE=database` (works fine for `sessions`/`jobs` since those tables exist in the real imported schema), but the `cache`/`cache_locks` tables — a Laravel 11+ convention — never existed in the old app's schema. Every `wire:click` goes through Livewire's checksum/rate-limiter, which reads the cache store, so clicking any interactive element fatal-errored: `SQLSTATE[42S02]: Base table or view not found: 1146 Table 'redesign_hrl.cache' doesn't exist`.

**Fix**: ran only the cache-table migration (`php artisan migrate --path=database/migrations/0001_01_01_000001_create_cache_table.php`), not a full `php artisan migrate` — the stock `users`/`jobs` migrations in the same scaffold would conflict with the already-existing real tables of those names.

## Incident: Lap Detail modal transition didn't play

First implementation used a PHP `@if` to conditionally render the modal — this meant every open/close was an instant DOM add/remove with no way for Alpine to animate a transition. Switched to Alpine `x-show` driven by `$wire.selectedDriverIndex !== null`, keeping the element always in the DOM.

That alone wasn't enough — the modal still opened instantly with no transition. Root cause: the element had no stable `wire:key`, so Livewire's DOM morph was recreating the node on every click instead of patching it in place, meaning Alpine saw a "new" element already in its final state with nothing to animate from. Fix: added a static `wire:key="lap-detail-modal"` (not keyed to the selected index). See [livewire-guide.md](livewire-guide.md) for the general pattern.

## Incident: sparkline tooltips clipped on first/last segments

Tooltips were centered under each sparkline segment via `left-1/2 -translate-x-1/2`. On the first/last segments (near the podium card's edges), the tooltip got visually clipped. Root cause: the podium card uses `.hud-clip` (`clip-path`), and `clip-path` clips **all** descendant overflow, not just elements with literal `overflow: hidden` — including absolutely-positioned tooltips extending past the card's box.

First fix attempt: manually special-case `left-0`/`right-0` for first/last segments — worked, but doesn't generalize to other clipped containers.

**Final decision**: adopted Alpine's official Anchor plugin (`@alpinejs/anchor`, wraps Floating UI) for real dynamic positioning with automatic overflow avoidance, registered via the `alpine:init` event since Livewire bundles its own Alpine instance (no standalone `Alpine.start()` in this app). See [livewire-guide.md](livewire-guide.md) for the usage pattern — this is now the standard approach for any future tooltip/popover.

## Sticky header background: match, don't replicate

The Lap Detail modal's sticky split-rows column header needed a background matching the surrounding gradient panel. First attempt hardcoded a duplicate copy of the panel's gradient values directly on the sticky element — this is fragile (two places to update if the panel's colors ever change) and also didn't look right (a locally-reproduced gradient isn't the same as a color that's actually part of the same continuous background). **Decision**: added `--color-hud-modal-start`/`--color-hud-modal-end` as real theme tokens in `resources/css/app.css`, referenced by both the panel and the sticky header — then further reverted the sticky header specifically to a **flat** color (`bg-hud-modal-start`, no gradient) per explicit follow-up, since a two-stop gradient repeated on a thin sticky bar wasn't actually what "match the surrounding" meant. Lesson: when two elements need "the same" background, both should reference one shared token — never duplicate a hex/gradient definition.

## Player lap popup: from "simpler" to full split comparison

First pass deliberately built the player lap-detail popup **without** a split comparison table, reasoning that "vs a record holder" doesn't naturally apply to a player's own personal lap log. It used its own standalone `$selectedLapIndex`/`openLap()`/`closeLap()` rather than the shared trait.

**Reversed** on explicit request: "show the splits against the map records in this instance." Once the scope actually matched the leaderboard modal's, switched to reuse `HasLapDetailModal` (rather than build a third near-identical mock-comparison implementation) and added `recordHolder`/`recordTag`/`recordTime` fields to the mock lap data to drive a "vs MAP RECORD" reference line.

Lesson: don't force premature reuse when scope looks different — but don't resist reuse once scope actually converges, either.

## Custom breakpoint scale replaces Tailwind defaults

Decision: fully replace Tailwind's default breakpoint scale (`sm`/`md`/`lg`/`xl`/`2xl`) with a project-specific named scale (`mm` 375px, `ml` 500px, `tp` 735px, `tl` 1020px, `d` 1270px, `dlg` 1440px, `dxlg` 1800px, `dxxlg` 2160px), via `--breakpoint-*: initial;` + redefinition in `resources/css/app.css`'s `@theme` block.

At the time of this decision, 52 existing usages of `sm:`/`md:`/`lg:` were already spread across 13 view files (every list/leaderboard page's desktop-vs-mobile table/list toggle, the nav's mobile overlay breakpoint, a couple of grid-column reflows). All were migrated onto `tp:` (735px), since they all served the same conceptual "desktop vs. mobile" split despite being defined at three different old breakpoint values (640/768/1024px) — consolidating onto one named breakpoint was judged more consistent than preserving the old values' exact pixel thresholds. See [architecture.md](architecture.md#breakpoints) for the full scale and [coding-standards.md](coding-standards.md) for the going-forward rule.

**Follow-up rule** (same session): two arbitrary pixel breakpoints already existed on the Servers featured-card layout (`max-[550px]:w-full`, `max-[360px]:w-full`, added just before the named scale existed). Decision: **never use hardcoded pixel breakpoints going forward** — snap to the nearest named value instead (`max-[550px]:` → `max-ml:` [500px], `max-[360px]:` → `max-mm:` [375px]), using Tailwind's auto-generated `max-{name}:` variants. If a genuinely new fine-tuning point recurs, extend the named scale rather than reaching for another one-off pixel value.

## Test/analysis tooling set up (Pest, PHPStan/Larastan, Rector, Semgrep)

Decision: get static analysis and testing tooling in place proactively, before real backend work starts, rather than bolting it on afterward.

- **Pest**: added the first real automated test (`tests/Feature/RoutesTest.php`, dataset-driven over every route), directly replacing the manual curl-and-grep verification process used throughout the earlier build (see the git history of this doc for how much of that there was). Left the stock Laravel installer example tests in place rather than deleting them, per the "don't delete tests without approval" convention — they're harmless boilerplate, not something worth a permission round-trip over.
- **PHPStan + Larastan**: installed at level 5 deliberately, not higher — the codebase is still frontend-on-mock-data, and the mock Livewire components return untyped arrays by design (see [coding-standards.md](coding-standards.md)'s mock-data section). A stricter level right now would mostly flag that intentional pattern rather than real problems. Passes clean at level 5 as of setup. Raise the level as real Eloquent models replace the mock arrays.
- **Rector**: configured with Laravel 13 + PHP 8.3 rule sets, `HasLapDetailModal` explicitly excluded (`withSkip`) since it leans on Livewire-specific dynamic property conventions Rector might "fix" incorrectly. Dry-run proposes 16 files' worth of changes (mostly `declare(strict_types=1)` + PHP 8.3 `#[Override]` attributes) — **not applied**, since "set the tool up" and "apply every suggested rewrite" are different asks; applying is a separate, deliberate follow-up action.
- **Semgrep**: this is the one tool that **could not actually be installed** in this sandbox. Semgrep ships only via pip, Homebrew, or Docker — this environment has no pip module (`ensurepip` itself is missing from the Python install), no passwordless sudo (so no `apt install python3-pip` either), no Homebrew, and no Docker. Checked GitHub releases directly too — Semgrep doesn't publish standalone binary assets there, only via PyPI. Wrote `.semgrep.yml` with a small set of custom rules anyway (unescaped Blade output, raw-SQL interpolation, hardcoded-credential-like assignments, a Livewire `mount()` validation reminder) so it's ready to run the moment someone has access to one of those install paths (e.g. CI). **These custom rules are unvalidated** — never actually run against real Semgrep here, so treat them as a first draft, not a proven ruleset.

Composer scripts added: `analyse` (PHPStan), `rector-dry`/`rector-fix`, `lint` (Pint + PHPStan), `check` (lint + rector-dry + test) — see [testing.md](testing.md).

## Global Player Ranking spec — ambiguity resolutions

The initial feature spec (as given) had a genuine overlap/contradiction and several unstated details that would each produce a different (and each individually reasonable) algorithm if guessed silently. Rather than pick silently, these were confirmed explicitly before writing [global-ranking.md](global-ranking.md):

- **Band boundary**: spec said "11–25" and "25–50" (25th place in both). Resolved to **11–25 and 26–50** — no overlap.
- **Interpolation shape**: spec just said "gradually decreases." Resolved to **linear within each band**, but with band 11–25 steeper than band 26–50 (continuing the pattern already visible in the explicit 1st–10th table, which itself eases from a 5-point gap near the top to a 2-point gap by 10th) — not a single curve across the whole 11–50 range, and not exponential/other non-linear shape.
- **Global Score definition**: not stated at all in the original spec. Resolved to **sum of per-map points**, not average — average would work against the stated "encourage participation across multiple maps" goal (a player who only plays one map perfectly could otherwise outscore a player who plays ten maps well).
- **Per-map lap-time ties**: not addressed in the original spec, but the overall tie-break rules referenced "earliest achievement date," implying recency/order already mattered somewhere. Resolved to **earliest lap wins the higher per-map position** (not shared/standard-competition-ranking positions), extending that same principle down to individual map rankings for consistency.
- **Leaderboard scope** (global vs. nested): not asked as a clarifying question — inferred from the spec's own stated purpose ("overall measure... across all race maps") to mean the **global, all-servers leaderboard** per map, not the server-scoped nested one. Flagged in the doc as an assumption to confirm, not a fully-resolved decision like the four above.

## Most Active Server spec — resolving formula-vs-goals tensions

The Most Active Server spec (as given) had the formula literally contradict two of its own stated design goals — worth confirming explicitly rather than writing a spec doc that documents its own internal contradiction. Written up in [most-active-server.md](most-active-server.md); resolutions:

- **"Valid Laps" counted raw, or capped?** The formula as given (`Valid Laps × 1`) would let one player inflate the score without bound by grinding the same map repeatedly — directly contradicting the stated goal "prevent one player repeatedly racing from dominating the score." Resolved: **"Valid Laps" now means distinct (player, map) participations**, not raw lap submissions — repeat laps on an already-played map add nothing further. Mirrors the same "count participation, not volume" principle [global-ranking.md](global-ranking.md) already uses (best lap per map, not every lap).
- **All-time cumulative or rolling window?** Spec didn't say, but an all-time cumulative score can only ever grow, which directly contradicts the stated goal "allow inactive servers to fall off the list over time." Resolved: **all three base metrics (Unique Players, Valid Laps, Maps Played) are computed over a rolling 90-day window**, not all-time — this is what actually makes "falling off" possible. The Recency Bonus (7/30/90-day tiers) stacks on top of this windowed base for extra emphasis on very recent activity. Noted in the doc: since the base window and the top recency-bonus tier are both 90 days, that top tier (+20) ends up applying to nearly any server with a nonzero base score — a minor redundancy, not fixed, just flagged for whoever tunes these numbers later.
- **Lap validity**: the schema has no validity/anti-cheat flag at all. Rather than invent a criterion, this was explicitly **deferred as a future problem** (per direction) — a map-file checksum to detect modded maps is one idea floated, though it wouldn't catch script-based cheats (hog-jumps, portals, etc.). Until that exists, every recorded lap is treated as valid for this algorithm.

This spec also **supersedes** an earlier, much simpler placeholder "most active server" definition that had been sitting in [database.md](database.md) since the initial schema-reconciliation pass (ranked by raw `lap_times` row count in a rolling 30-minute window) — that placeholder predates this full spec and is now retired in favor of it.

## Server Single page spec — docs-only by design, and reusing Global Ranking

Explicitly confirmed as a docs/planning task before writing anything, since the request read ambiguously between "spec this" and "build this now" — the previous two turns (Global Player Ranking, Most Active Server) were both pure planning, but this one described concrete page sections for an *already-built* real page (`ServerShow`), which could plausibly have meant "add these now, mock-data-first, like the rest of the frontend." Confirmed docs-only before proceeding, consistent with the session's stated framing. Written up in [server-single.md](server-single.md).

**"Top Players" reuses the Global Player Ranking algorithm**, not a new one — the request's own phrasing ("Based on our Global player ranking algo, we can add the following") was read as a direct instruction to reuse the formula, just scoped to one server's nested leaderboard positions instead of the global leaderboard, summed only across maps played on that server. Named **Server Score** to distinguish it from Global Score (same formula, different scope) — see [glossary.md](glossary.md). Implementation should keep the points table/interpolation logic in one shared place so Global Score and Server Score can't silently drift apart from duplicated formula code.

Two items were deliberately **left open rather than guessed**, since both have meaningfully different implementations depending on the answer:
- **"Number of records set"** — could mean "maps where this server currently holds the course record" (cheap, current-state query) or "how many times has a record been set here historically, even if since beaten" (expensive, requires point-in-time replay of full lap history). Both are legitimate readings of the phrase; picking one silently would bake in a potentially-wrong and expensive-to-reverse assumption.
- **"Average lap time"** (for Top Players) — raw average across every attempt vs. average of per-map personal bests. These produce different numbers with different meaning (practice-inclusive pace vs. skill ceiling).

## Players List redesign — validates existing design, surfaces one new hard question

Continued the same docs-only pattern as the previous three feature-spec turns without re-confirming — by this point in the session the pattern was well-established (two pure-algorithm specs plus one real-page spec, all docs-only), so re-asking would have been redundant rather than careful.

The request's own stated principle — "we don't want to just show raw lap counts, as grinders will dominate, it doesn't reflect skill, it dilutes the leaderboard's meaning... prioritize score, records, consistency, and activity" — is a direct, independent validation of a design choice [global-ranking.md](global-ranking.md) already made (position-based points, capped per map, rather than raw lap volume). Worth noting as confirmation, not just a new requirement: the earlier design reasoning holds up against a fresh, differently-worded statement of the same concern.

Two smaller things resolved along the way:
- **Global Player Ranking's own open "UI surface" question is now resolved**: `/players` becomes the Global Leaderboard. That open item in [global-ranking.md](global-ranking.md) is marked done.
- **New evidence on the still-open "number of records set" question** (first raised in [server-single.md](server-single.md)): this page's own framing ("make the page feel alive, instead of static") points toward the historical-events reading, since the current-state reading reduces to a mostly-static number system-wide. Recorded as a hint in [roadmap.md](roadmap.md), not treated as a final decision — the underlying question (cheap-and-static vs. expensive-and-dynamic) is still open and still worth a real answer rather than inferring one from page-copy tone alone.

One genuinely new hard question surfaced: the **Trending indicator** is the first planned feature in this project that plausibly can't stay within the "fully derivable from current data, no stored history" philosophy every other feature so far has followed ([database.md](database.md), [global-ranking.md](global-ranking.md), [most-active-server.md](most-active-server.md) are all explicitly designed to avoid this). A literal "did this player's rank move" comparison needs *some* prior state to compare against. Documented two paths (periodic snapshots vs. a recent-activity proxy) without picking one — this is a real architectural fork, not a copy-detail, and deserves a deliberate answer when it's actually built.

## Player Single expansion — reused definitions over new ones wherever possible

Continued the same docs-only pattern. This spec was deliberately written to **reuse already-established definitions rather than mint new ones** wherever the request's terms mapped onto something already decided:
- "Num Records" → the same current-state "Records" definition from [players-list.md](players-list.md), not a new one.
- "Top 3 finishes" → literally [global-ranking.md](global-ranking.md)'s tie-break rule 2, now surfaced as a visible stat instead of a hidden tiebreaker input. Worth noting this kind of reuse as a sign the earlier specs are holding together as a coherent system, not just individually plausible documents.
- "Maps completed" → same count as [players-list.md](players-list.md)'s "Maps with PBs," different label.

One new **terminology clash** was caught and flagged rather than silently creating ambiguity: "Total valid laps" here means a **plain raw lap count**, whereas "Valid Laps" on the Most Active Server page ([most-active-server.md](most-active-server.md)) means "distinct (player, map) participations" — a deliberate redefinition made there specifically to prevent gaming that particular score. There's no score being protected on this page, so the raw-count reading is correct here, but it's the same English phrase meaning two different things across two docs — recorded explicitly in the glossary so nobody assumes the two are interchangeable.

"Best Performance" ("top 3 laps or biggest achievements") was left as an explicit open item rather than picking one — the spec's own wording offered two genuinely different features (a trivial top-N query vs. an editorial "achievements" concept needing its own definition of "biggest"), and picking silently would have hidden that second option needs real design work before it's buildable.

This page's expansion also differs from the previous three specs in one respect: it builds around **existing, already-working code** (the lap log + `HasLapDetailModal` popup), not a wholly greenfield feature — noted explicitly in the doc so new sections get wired into that existing interaction pattern rather than duplicating it.

## Homepage: block-selection mechanism confirmed, one long-open question effectively resolved

Two things needed a real decision before this spec could be written coherently, both asked explicitly rather than assumed:

- **Block selection isn't just "hide empty ones"** — since most of the 6 candidate highlight blocks will almost always have *some* data (there's basically always a most-active server, always live stats), a pure empty/non-empty filter wouldn't naturally narrow anything to 3. Confirmed: a **fixed priority order** (Records > Most Active Server > Fastest Improvements > New Content > Achievements > Live Stats), take the first 3 that have something to show — not a computed per-load "interestingness" score, which would have needed its own scoring formula per block type.
- **"Live Stats Snapshot" vs. "Quick Stats" overlap** — confirmed these are genuinely different, not redundant: Quick Stats is simple all-time totals (always shown, outside the 3-block system), Live Stats Snapshot is windowed/recency-focused (30d/90d/today) and competes for one of the 3 highlight slots like the other 5 candidates.

**The long-open "number of records set" question (first raised in [server-single.md](server-single.md), hinted at again in [players-list.md](players-list.md)) is now effectively forced toward the historical reading**, not just hinted at: the homepage's "Latest / Current Records" block needs to show discrete recent record-setting events (map, player, time, server, "1h ago") — something the current-state reading (which only knows "who holds the record right now," not "when they took it") has no way to produce. This doesn't mean every instance of "records" across every doc must use the historical definition (per-page context still matters — see [players-list.md](players-list.md)'s note that its own "Records" column deliberately stays current-state), but at least one real feature now structurally requires the historical derivation to exist.

**A related but distinct question got clarified along the way**: "largest rank jump" and "new Top 10 entry" (Fastest Improvements block) sound like they need the same kind of historical tracking as the still-unresolved Trending indicator ([players-list.md](players-list.md)), but they don't — both are derivable by comparing current state to "current state minus the player's most recent qualifying lap," a stateless re-computation, not a stored point-in-time snapshot. The Trending indicator's problem is specifically wanting comparison against an arbitrary *past* point (e.g. "7 days ago"), which one-event-back comparison can't answer. Worth keeping these conceptually separate — "needs a before/after comparison" doesn't automatically mean "needs stored history."

Also added a **Product Pillars** concept to [scope.md](scope.md) (Map=Performance, Server=Community, Player=Mastery+Consistency) — this originated as homepage framing but clearly applies app-wide, so it was placed in the project's scope doc rather than left buried in a page-specific spec where later work wouldn't think to look for it.

## Roadmap re-prioritized: frontend design before real algorithms/data

After writing four page specs ([server-single.md](server-single.md), [players-list.md](players-list.md), [player-single.md](player-single.md), [homepage.md](homepage.md)) that all reference not-yet-built backend features (Global Player Ranking, Most Active Server scoring, historical record-events), the natural next question was sequencing: build the real algorithms first, or the page UIs first?

**Decided: frontend first, static/mock data throughout**, matching the exact sequencing already used for every other page in this project (Servers, Maps, Players were all built mock-first — see the early Progress Log history). There's no principled reason for these four specs to break that pattern just because their mock numbers happen to be described by a written algorithm doc instead of invented ad hoc like the earlier pages' sample data was. [roadmap.md](roadmap.md)'s "Next up" section was restructured into two explicit phases (frontend design, then backend/real data) rather than one interleaved list, so this ordering is visible at a glance rather than something you'd have to infer from reading every line.

One addition beyond a pure reorder: **Homepage's mock data should include mock "current state" inputs** (fake recent records, improvements, new content) specifically so the block-*selection* logic (which 3 of 6 highlight types show) can actually be built and reviewed — not just the visual design of whichever one block someone arbitrarily chose to mock. A page whose main feature is "pick the 3 most relevant things" needs varied mock inputs to meaningfully exercise that feature at all.

## Homepage built (Phase 1, item 1): `/` becomes a Livewire component, real selection logic over mock data

Replaced the old `Route::get('/', fn () => view('welcome'))` closure with `App\Livewire\Home`, following the same `#[Layout(...)]` full-page-component pattern as every other route.

**Section order, per explicit user feedback**: Quick Stats, then Latest Highlights, then Quick Links — Quick Stats moved to the top rather than sitting between Highlights and Links, so the reader sees the simple always-true numbers before the curated/selected content.

**Old site content (Future Plans / Known Issues / Changelog)**: initially left in `welcome.blade.php`, unrouted, pending a decision on where it should live (a `/about` page was floated as a candidate). Per explicit user feedback ("keep our old information we had at the bottom"), this was instead folded directly into `home.blade.php` as three sections after Quick Links — Future Plans / Known Issues (2-column) and Changelog (dated list) — using the exact same markup/content as before. `welcome.blade.php` was then deleted as fully redundant rather than left as dead code duplicating content that now lives in `home.blade.php`.

Per further explicit feedback ("keep... but separate from the above content"), a labeled divider (`— ABOUT & CHANGELOG —`, two flex-grown hairlines either side of a faint centered label) was added directly above the Future Plans section, so the page reads as two distinct zones: live community data (Quick Stats/Highlights/Links) above, static site/project info below — not one undifferentiated scroll.

## Server Single page additions built (Phase 1, item 1)

All four planned additions from [server-single.md](server-single.md) were added to `ServerShow`/`server-show.blade.php`, in spec order (Stats Card, Top 3 Fastest Laps, Top Players, Maps unchanged, All Laps), all on mock data:

- **Stats Card**: Total laps, Total players, Records set. "Records set" uses the doc's default assumption (current-records-held, not historical-record-breaking-events) — still not formally decided, mock number either way.
- **Top 3 Fastest Laps**: raw times across all maps on the server (not per-map), rank-colored green/cyan/gold to match the existing podium convention elsewhere, map name shown prominently since times aren't otherwise comparable across different maps.
- **Top Players (Server Score)**: reuses the green/cyan/gold rank convention; "Average lap time" uses the doc's default assumption (raw average across all attempts, not average-of-per-map-bests) — also still not formally decided.
- **All Laps**: this is the first table in the project built with **real pagination**, not a static mock footer like the earlier "‹ PREV · 1 2 3 · NEXT ›" placeholder on the Map Leaderboard partial. `ServerShow` uses Livewire's `WithPagination` trait for page-state management and manually constructs an `Illuminate\Pagination\LengthAwarePaginator` by slicing a 47-row mock array — there's no Eloquent model yet to paginate against directly, but the pagination *mechanism* (page query string, prev/next wiring, "showing X–Y / Z") is real and will keep working unchanged once `$this->allLaps` is replaced with a real paginated query. Verified by rendering `/servers/1?page=4` and confirming it correctly shows rows 46–47 of 47.

Neither of the two "not decided" definitional questions (records-set reading, average-lap-time reading) were resolved by this work — building the UI didn't require resolving them, only picking *a* consistent mock convention, which was the whole point of doing this frontend-first. Both remain open for Phase 2.

## Podium partial extracted, Server Single's Top 3 sections switched to it

Per explicit user feedback ("the top 3 fastest should use the podium style, always... ideally, always follow the structure as per the map single"), extracted the "3 clipped cards, green/cyan/gold, 1st bigger and centered" visual out of `leaderboard-podium-and-table.blade.php` into a standalone `resources/views/livewire/partials/podium.blade.php`, taking a generic `$podium` array (`title`/`subtitle`/`value`/`meta`/`badge` per position, no click handling, no sparkline). Server Single's Top 3 Fastest Laps and Top Players (Server Score) sections were rebuilt from flat ranked lists to use this shared partial instead — this is the "second/third genuine use case" extraction trigger that [coding-standards.md](coding-standards.md) had been flagging as pending since the Homepage/Players List specs were written.

**Deliberately not touched**: the Map Leaderboard's own podium markup stays as its own copy rather than being rewired onto the shared partial — it needs the lap-detail-modal `wire:click` open behavior and the split-time sparkline on the 1st-place card, neither of which the generic partial supports (or should be forced to support, since neither is relevant to Server Single's plain stat cards). The two are kept visually identical by shared class conventions, not by literally sharing one file.

**Deliberately not touched either, confirmed by the user**: Homepage's Most Active Server highlight block keeps its existing compact ranked-list style rather than switching to the podium partial too. That block lives inside one of three narrow highlight cards in a `tl:grid-cols-3` grid — the podium's `tp:grid-cols-[1fr_1.28fr_1fr]` layout assumes something closer to full content width (as Server Single's and Map Leaderboard's podiums both get) and would look cramped/broken squeezed into a single grid cell at that width. Raised as a judgment call, user confirmed: "Nah, homepage stays as [is]." Not a candidate for the shared podium partial going forward.

## Server Single's "Top 3 Fastest Laps" removed

Shortly after building it (podium-styled, per the entry above), the user reconsidered: "Doesn't really make sense on this page... different maps have drastically different lap times (from 60–120s)." The section's own spec text had actually already flagged this exact tension ("compares raw times across maps of different lengths/layouts, which is intentional per the request") but on seeing it rendered, the raw-time comparison read as meaningless rather than intentional.

Asked how to proceed rather than guessing a normalized replacement (e.g. "biggest margin over 2nd place," "most course records held") — **decided: remove the section entirely**, no replacement. Top Players (Server Score) and Maps already cover the server's interesting rankings without needing a cross-map raw-time comparison. Removed from `ServerShow`/`server-show.blade.php` and from [server-single.md](server-single.md) (moved to a new "Removed" section in that doc rather than silently deleted, so the reasoning survives for anyone wondering why the spec doesn't match a four-section description anymore). The shared `podium.blade.php` partial extracted for this section stays in place — Top Players still uses it.

## Small stat displays standardized on the Quick Stats badge-pill style

While reviewing the above, the user flagged that Server Single's Stats Card (a `hud-clip` bordered card with 3 big numbers) should instead match Homepage's Quick Stats style (small bordered badge-pills in a wrapped row) — and stated this as a **standing convention**: "these small/simple stats will always be styled like this," not a one-off preference for this one card.

Restyled Server Single's Stats Card to the exact Quick Stats markup (same classes, same `flex flex-wrap gap-2.5` pill row). Documented as a standard in [coding-standards.md](coding-standards.md) under a new "Stat displays" section, with the distinction drawn explicitly: this pill style is for **simple standalone all-time totals**; the bigger bordered-card-with-large-numbers treatment (e.g. Homepage's Live Stats Snapshot, which shows paired 30d/90d and today/week figures) is a different kind of display for windowed/paired stats, not the same pattern at a different size — so it wasn't touched.

**Immediately refined further**: "no need for the header, make it like the badges on the map single (just below the title)" — moved Server Single's stats badges to directly under the `<h1>` (above the tagline paragraph), dropped the `// STATS` section header, and switched to the Map Leaderboard's exact badge classes (no `bg-hud-green/4` fill, `tracking-[0.14em]` not `[0.12em]`). Applied the same refinement to Homepage's Quick Stats for consistency (moved above its tagline, header dropped) — **then reverted**, per explicit follow-up: "Don't touch homepage, as was is correct." Homepage's Quick Stats is back to its original form (own `// QUICK STATS` header, positioned after the intro paragraph, `bg-hud-green/4` fill, `tracking-[0.12em]`) and should be treated as a settled, separate variant, not an inconsistency to "fix" later — see [coding-standards.md](coding-standards.md)'s "Stat displays" section, which now documents both variants explicitly rather than implying one style should replace the other everywhere.

## Players List redesigned into a Global Leaderboard (Phase 1, item 2)

Built per [players-list.md](players-list.md), all on mock data (Global Score itself is still Phase 2 work):

- **Info card**: 4 stats (Total players, Active 30d, Records set, Avg maps/player), styled with the Server Single variant of the stat-badge pattern (no header, directly under the `<h1>`) — this is now the default for new pages per [coding-standards.md](coding-standards.md), not a fresh decision. "Records set" here uses the historical-record-breaking-events reading per the doc's own leaning, deliberately different from the table's per-player "Records" column below it (current-state reading) — both definitions coexisting on one page is explicitly called out as intentional in the spec, not an inconsistency.
- **Top 3 podium**: uses the shared `podium.blade.php` partial (see the Server Single entry above) — this is its second real consumer, exactly the pattern the extraction was meant to serve.
- **Table (rank 4+)**: 7 columns per spec. The **Trend** column needed a new small shared partial, `resources/views/livewire/partials/trend-indicator.blade.php` (▲ green / ▼ red / – faint, with an optional rank-delta number) — built with mocked `direction`/`delta` values per player; the real *mechanism* behind what "trending" means (periodic snapshots vs. a recent-activity proxy) is still an open, undecided question per the spec. Building the UI didn't require resolving it, only deciding what an arrow looks like.
- Player row links were built with an explicit `id` field distinct from `rank` (`array_map` now sets both) — a small correctness fix so the mock table doesn't accidentally treat leaderboard position as if it were the player's identity, which would silently break once real player IDs exist.

## Player Single expanded, completing Phase 1

Before building, flagged a conflict with the user rather than proceeding on autopilot: [player-single.md](player-single.md)'s "Best Performance" section defaulted to raw top-3-fastest-laps (option 1), which has the exact same cross-map-time-comparability problem that just got Server Single's "Top 3 Fastest Laps" removed — a player's fastest lap on a 60s map and their fastest lap on a 120s map aren't comparable any more than two different servers' times were. Asked how to handle it; **decided: curated achievements list (option 2)** — a short editorial list (records held, strong finishes, standout maps) instead of a ranked-by-time list. No real "biggest achievements" selection algorithm exists yet; mock content only.

Built all remaining sections from [player-single.md](player-single.md) on top of the pre-existing, already-working lap table + Lap Detail popup (`HasLapDetailModal`), per the spec's explicit instruction not to build a second lap-detail mechanism:

- **Player Info header**: added Global Rank/Score badges below the title, matching the now-standard Server Single stat-badge convention.
- **Stats Card**: 7 stats (records, top-3 finishes, maps completed, servers played, total laps, first seen, last active), same badge convention with its own `// STATS` header since it isn't the page's title-adjacent badges.
- **Performance by Map**: this *is* the pre-existing per-map-best-lap table, extended in place with Map Rank and Points columns rather than built as a separate table — the existing rows already were exactly "one row per map, this player's best lap," which is precisely what the spec's Map/PB/Map Rank/Points/Server table needed. The Lap Detail modal wiring (`openLap($index)`) was untouched.
- **Fav Servers**: new table (Server/Laps/Best Rank), sorted by lap count per the spec's stated assumption — not a modal-linked table, just a plain list.
- **Recent Laps**: reuses the *same* `$laps` array/index as Performance by Map, rather than introducing a second dataset and a second selected-index state variable. This is an honest simplification, not a hidden gap: with only one mock lap per map today, "most recent laps" and "per-map personal bests" happen to produce the same rows — at real scale (many laps per map) these would diverge into genuinely different queries (one row per submission, chronological vs. one row per map, best-only). Documented as an open item in [player-single.md](player-single.md) rather than silently treated as done-for-real.

## Eloquent models built (Phase 2, item 10), surfaced a real data constraint

Built one model per real table — `Server`, `Map`, `Player`, `LapTime`, `LapTimeSplit`, `PlayerClaim`, `ServerClaim` — plus factories, no migrations (all tables already exist). Followed the project's existing Laravel 13 convention (seen in the pre-existing `User` model) of PHP attributes (`#[Fillable]`) and a `casts()` method rather than `protected $fillable`/`protected $casts` properties.

`users_players`/`users_servers` (the claim-code tables) were modeled as first-class models (`PlayerClaim`/`ServerClaim`) rather than forced into `belongsToMany` pivots — both tables have their own `id`, `claim_code`, `claimed_at`, and `deleted_at` (soft deletes), which is richer than a plain pivot table and awkward to express well through Eloquent's pivot API. `servers_maps`/`players_servers`, by contrast, are genuinely plain two-FK pivots with nothing beyond timestamps, so those stayed as ordinary `belongsToMany` relationships with no dedicated model.

Verified every model and relationship against the **real** `redesign_hrl` data (not factories) via Tinker: 3 servers, 10 maps, 817 players, 1657 lap times, 429 splits, 1 player claim, 1 server claim — all relationships (`Server::maps`, `Map::lapTimes`, `LapTime::with(['server','map','player','splits'])`, `PlayerClaim::with(['user','player'])`, etc.) resolved correctly.

**Real finding while verifying**: `lap_times.created_at`/`updated_at` are `DATE` columns, not `DATETIME`/`TIMESTAMP` — confirmed both via `SHOW COLUMNS` and by observing that every real row's `created_at` reads as exactly midnight. This directly contradicts several already-written mock UI strings that assumed hour/minute-level recency ("38m ago" on Homepage's Latest Records mock, "12m ago" on Player Single's Last Active). `LapTime`'s `created_at`/`updated_at` were explicitly cast to `'date'` (not Eloquent's implicit `'datetime'`) so the model's own type signature is honest about this rather than silently implying more precision than the data actually has. Documented as a new open question in [roadmap.md](roadmap.md) and a full breakdown in [database.md](database.md).

## Lap timestamp precision fixed going forward

Immediately after the above finding, the user decided: **"We'll add support for time to the data for future records. This can just be the time we received the record, as it's close enough."** — i.e. don't invent a separate precise-submission-time field or wait on the webhook payload inspection; just widen the existing `created_at` column so future inserts get real timestamp precision, and treat "time the row was inserted" as an acceptable proxy for "time the lap happened."

Built and ran `widen_lap_times_timestamps_to_datetime`, changing `lap_times.created_at`/`updated_at` from `DATE` to `TIMESTAMP` via `->change()` (confirmed working without `doctrine/dbal` installed — dry-ran with `--pretend` first to check the generated SQL before touching real data, then ran scoped to just that one migration file per the project's standing "don't run `php artisan migrate` blindly" caution). Verified: all 1657 existing rows preserved unchanged (still reading as midnight — MySQL doesn't invent data), and a test insert (wrapped in a transaction, rolled back afterward) confirmed new rows now get a real `H:i:s` timestamp. Removed the `'date'` cast from `LapTime` back to Eloquent's default `datetime` handling, since the column can now actually hold that precision.

**What this does and doesn't fix**: going forward, any lap submitted after this migration (2026-07-05) has real time-of-day precision. Historical rows before that point are permanently day-only — there was never a time-of-day to recover. This means relative-time displays ("3h ago") will be genuinely accurate for new laps but could look misleadingly precise for old ones if a future implementation isn't careful to format historical (pre-migration) records at day-level rather than hour/minute-level. Flagged as a display-logic nuance for whoever builds the historical-record-breaking-events derivation (roadmap.md item 13), not a blocker for anything currently in progress.

## First page wired to real data (Server Single), and what that actually took

Started Phase 2 item 8 with `ServerShow` (Server Single) — Maps section, Stats Card, and All Laps now query `Server`/`Map`/`LapTime` directly instead of mock arrays. Top Players (Server Score) stays mock, since it depends on Global Player Ranking (item 11), not yet implemented.

**Discovered along the way, before any of the real-data wiring could even be tested**: the project's test suite runs against an in-memory SQLite database built entirely from `database/migrations` — and until now, none of those migrations created `servers`/`maps`/`players`/`lap_times`/etc., because those tables already exist in the real dev DB and were deliberately never migrated (per the project's "don't run `php artisan migrate` blindly" caution). That caution was correct for the *real* DB, but it meant the *test* DB had no schema for any of these tables at all — the moment `ServerShow` ran a real `Server::findOrFail()` query, `RoutesTest`'s `/servers/1` case failed with "no such table: servers," because Pest's `RefreshDatabase` (commented out project-wide, unused until now) had nothing to migrate.

**Fixed by writing `create_*_table` migrations for every real table** (`maps`, `servers`, `players`, `lap_times`, `lap_time_splits`, `servers_maps`, `players_servers`, `users_players`, `users_servers`), matching the real schema's exact column types — these are meant to run in fresh environments (CI, a new dev machine's test suite) only, never against the already-populated real dev/prod database. Ordered via explicit sequential timestamps (not `make:migration`'s default same-second timestamps, which alphabetized `lap_times` before `players` and would have broken the foreign keys). `RoutesTest.php` now uses `RefreshDatabase` and seeds a `Server::factory()->create(['id' => 1])` in `beforeEach()` so `/servers/1` has something real to resolve.

**Two more real-data landmines found while building this**:
1. **`servers_maps` and `players_servers` have massive duplicate rows** — one server had 207 `servers_maps` rows for only 9 distinct maps, and 201 `players_servers` rows for only 17 distinct players. Almost certainly the old app inserted a new pivot row every time it *saw* a pairing again rather than upserting. Fixed by adding `->distinct()` directly inside `Server::maps()`/`Server::players()`/`Map::servers()`/`Player::servers()`'s relation definitions, so any consumer gets correctly deduplicated results automatically. Caveat kept in code comments and [database.md](database.md): a bare `->distinct()` is NOT honored by the `.count()` aggregate shortcut (it silently returns the un-deduplicated count) — must use `->distinct('table.id')->count('table.id')` instead. `ServerShow`'s Stats Card sidesteps this entirely by counting distinct players via `lap_times` directly (`LapTime::where('server_id', ...)->distinct('player_id')->count('player_id')`) rather than trusting the pivot table's count, since that's both more semantically correct (a player only "played" if they have a lap) and avoids the pitfall.
2. **The real `users` table's `id` column is `int unsigned`**, not `bigint unsigned` — but this project's *stock* Laravel `users` migration (the framework default, already in the repo before this session) creates `id()` as `bigIncrements`. This is a real, pre-existing mismatch between the imported production schema and the framework scaffold, not something introduced now. Also the real `users` table has an `api_token` column the stock migration doesn't create. Not fixed — out of scope for this pass (nothing currently being wired touches `User`) — but `players.user_id`/`users_players.user_id`/`users_servers.user_id` were all deliberately modeled as plain `unsignedInteger` columns rather than `foreignId()->constrained()` specifically to avoid a cross-type foreign key error from this mismatch. Flagged here as a real open item, not silently worked around.

**Also implemented for real, not left as an open question**: the Stats Card's "records set" now uses the current-records-held reading (comparing each map's best time on this server against that map's global-best across all servers) — the doc's stated default assumption, which turned out to be simple enough to just build rather than leave mocked once real models existed.

Verified the whole page against real data (not just the test schema) via Tinker against `/servers/7`: real server name, real per-map lap counts/best times, real stats (86 laps, 17 players, 0 records held), and a real paginated All Laps query — not just "does it 200," actual correct values.

**PHPStan then caught two more real things**, not suppressed: (1) every relation method needed an explicit `@return BelongsToMany<Map, $this>`-style generic — without it, Larastan can't infer what a relation's collection actually contains and flags every property access on the result as "undefined property on `Model`." Added generics to every relation across all models. (2) The `selectRaw`/`groupBy` aggregate queries for per-map stats (`MIN(time) as best`) return rows that aren't real `LapTime` records — they have a `best` column the model doesn't actually have. Fixed by calling `->toBase()` before `->get()`/`->pluck()` on those two queries, which drops to a plain query builder returning `stdClass` rows instead of mis-typed Eloquent models — the honest representation of what those rows are, not a workaround. `composer analyse` passes clean at 0 errors with these fixes; output values unchanged, verified by re-running the same Tinker check.

## Podium cards weren't links

The user asked "Is the podium linked?" — they weren't: `podium.blade.php`'s three cards were plain `<div>`s with no way to reach the player's page, unlike almost every other row/card elsewhere in the app.

First attempt at a fix was buggy and caught before landing: tried to pick the tag name dynamically via `<{{ $href ? "a href=...\"" : "div" }}>` inside Blade's `{{ }}` interpolation — but `{{ }}` HTML-escapes its output, so that would have rendered the literal escaped text `&lt;a href=...&gt;` instead of a real opening tag, breaking the markup entirely. Caught this before running it and rewrote the partial using a real `@if/@else` with two fully separate, hand-written blocks (one `<a>...</a>`, one `<div>...</div>`) per podium position — more repetition (9 total open/close pairs across 3 positions), but the only version that's actually valid. Documented the dynamic-tag pitfall in [coding-standards.md](coding-standards.md) so it isn't attempted again.

`podium.blade.php` now accepts an `href` key per position; renders as `<a>` when set, plain `<div>` otherwise (kept generic for any future non-linkable use, though every current consumer is player-based and always sets it). Wired `href` into both existing consumers: `ServerShow`'s Top Players (linking to the shared mock driver roster's ids — VORTEX=1, NeonHalo=2, GravLift=3, matching ids used elsewhere) and `PlayerList`'s top-3 podium (using each mock player's existing `id`). Verified by rendering both `/servers/7` and `/players` and confirming real `<a href="/players/{id}">` tags appear in the output, not just in the Livewire hydration payload.

## Server Single's "All Laps" renamed to "Latest Laps" — dedup tried, then reverted

The user renamed the section and initially asked for **one row per distinct (player, map) pair on this server — that player's most recent lap on that map — ordered by date**. Implemented with `ROW_NUMBER() OVER (PARTITION BY player_id, map_id ORDER BY created_at DESC, id DESC)` in a subquery (`->toBase()` on the `LapTime` query, then `DB::query()->fromSub(...)->where('rn', 1)`), player names/map labels bulk-fetched via `whereIn` after pagination since the subquery result is plain `stdClass` rows, not `LapTime` models. ANSI window-function syntax worked identically on MySQL 8 (real dev DB) and SQLite 3.25+ (test DB).

**Immediately corrected**: "this Latest Laps doesn't need to be distinct (Yes, someone might run 5 laps, but its a feed)." The section is meant to be an activity feed, not a curated one-row-per-pairing summary — a player running 5 laps in a row should show all 5, since that's what actually happened and is exactly the kind of thing a "latest activity" feed exists to show. **Reverted to a plain reverse-chronological query** (`LapTime::where('server_id', ...)->with(['map', 'player'])->orderByDesc('created_at')->paginate(15)`), same shape as the original "All Laps" implementation before the (wrong) dedup attempt — removed the now-unused `Map`/`Player`/`Carbon`/`DB` imports that only existed to support the subquery approach. Verified against real data: total count is back to 86 (the real raw lap count for server 7), with "Puddin" correctly appearing twice for repeated laps on the same map.

**Still true and still worth keeping**: the duplicate-player-identity finding from the dedup attempt (`docs/database.md` — "TAIIDOSH" is two separate `players` rows with different `hash` values) remains a real, documented data-quality observation, independent of whether this particular section deduplicates or not.

## Server Single's Stats badges split into 6, "records set" dropped entirely

The user asked for the Stats badges to be "split out": instead of one flat all-time Laps count and one all-time Players count, show **Laps** (all-time / 30d / 90d) and **Players** (all-time distinct / active 30d / active 90d) — six badges total, giving activity context rather than static totals. Also asked to drop "Records set" entirely: "I don't think we need records set, as this is more related to players" — not deferred, removed, since it's a players-list.md/player-single.md concept, not a server one.

Implemented via a small `$baseQuery` closure (`fn () => LapTime::where('server_id', $server->id)`) reused across all six counts, adding `->where('created_at', '>=', now()->subDays(N))` for the windowed variants and `->distinct('player_id')->count('player_id')` for the player counts (same duplicate-pivot-avoidance pattern as before — still derived from `lap_times`, not `players_servers`). Removed the now-unused `$globalMapBests` query and `$recordsSet` computation entirely, since nothing else used them.

Verified against real data (server 7): 86 all-time laps / 36 in the last 30 days / 38 in the last 90 days; 17 all-time distinct players / 9 active in 30 days / 11 active in 90 days. Removed the "Number of records set" open question from [server-single.md](server-single.md) and [roadmap.md](roadmap.md) — it's fully resolved now by removing the stat, not by picking one of the two competing definitions that had been debated for it. That definitional question still applies to [players-list.md](players-list.md) and [homepage.md](homepage.md), which weren't touched.

## Latest Laps' date column switched to relative time + hover tooltip

The user pointed out an absolute date/time ("01 Jul 2026 00:00") is ambiguous for an international audience — no visible timezone, so a viewer can't tell if that's their local time, the server's, or UTC. Asked for relative ("ago") phrasing instead, with a hover tooltip showing the exact date/time and a timezone abbreviation as a fallback for anyone who needs precision (used "NZT" as an example of the concept, not a requirement to display New Zealand time specifically — the app's actual configured timezone is UTC, per `config/app.php`, so that's what the tooltip shows).

Implemented: `ServerShow::laps()` now returns both `date` (`$lap->created_at->diffForHumans()`) and `dateExact` (`->format('d M Y, H:i').' '.->format('T')`, e.g. "01 Jul 2026, 00:00 UTC"). The tooltip reuses the exact same Alpine Anchor hover pattern already established for the split-pace sparkline (`x-anchor.top.offset.6`, `group`/`group-hover:block`) rather than inventing a new tooltip mechanism — applied to both the desktop table and mobile list variants of the date display. Documented as a standing convention in [coding-standards.md](coding-standards.md) for any other real date display built later (Player Single, Players List, etc. still show mock date strings today).

## Latest Laps pagination: Previous button had an inverted `@disabled` condition

The user reported: "Latest laps pagination goes forwards but not previous (shows disabled)." Found a real bug, not a Livewire/pagination-framework issue: the Previous button's condition was `@disabled(! $laps->onFirstPage())` — backwards. That disables Previous whenever the user is **not** on the first page (i.e. exactly when Previous should be clickable) and leaves it enabled on page 1 (where it should be the one disabled). Confirmed the underlying pagination itself was never broken — a direct Livewire component test (`nextPage` then `previousPage`) correctly moved 1→2→1 — the bug was purely the button's disabled-state condition being inverted, not the click handler or `WithPagination` wiring.

Fixed: `@disabled($laps->onFirstPage())` (dropped the `!`). Verified via `Livewire::test()` on both page 1 (Previous button now correctly has the `disabled` attribute) and page 2 (Previous button correctly has no `disabled` attribute). Checked the rest of the app for the same inverted-condition pattern — this is the only real Prev/Next pagination control that exists (every other "SHOWING X/Y" footer elsewhere is still a static mock label with no functional buttons), so the bug was isolated to this one spot.

Also added `cursor-pointer` to both Prev/Next buttons per explicit request — native `<button>` elements don't get a pointer cursor by default, unlike `<a>` tags, so this needed to be explicit (the existing `disabled:cursor-not-allowed` continues to correctly override it when the button is actually disabled).

## Latest Laps date tooltip was anchoring to the wrong element

The user noticed the exact-date tooltip appeared centered over the whole DATE column, not over the actual (right-aligned) relative-time text. Root cause: `x-anchor.top.offset.6="$el.parentElement"` referenced the `group relative` wrapper span, but that span was a **direct child of the `grid grid-cols-[...]` row** — CSS Grid blockifies direct children regardless of their original `display`, so that `<span>` was stretched to fill the entire 170px column, not sized to its actual text content. The tooltip correctly centered over that element — it just wasn't the element the user expected, since the visible text sits right-aligned at the column's edge, not centered in it.

Fixed by nesting an `inline-block` `group relative` span **inside** the column-width span, wrapping just the date text, and anchoring to that inner element instead. Applied to both the desktop (grid column) and mobile (previously combined "Map · date" text) variants — the mobile version now also anchors to just the date portion rather than the whole "Map · date" string, for the same reason. Documented as a sub-point under the existing tooltip convention in [coding-standards.md](coding-standards.md): `x-anchor`'s reference element must tightly fit the hovered text, not a layout container that happens to contain it.

## Latest Laps rows now open the shared Lap Detail modal

The user asked for Latest Laps rows to open "our lap modal from elsewhere" on click. Two different modal variants already existed in the app, and neither was a perfect fit as-is:
- `lap-detail-modal.blade.php` (shared partial, used by Map Leaderboard/Server Map Leaderboard) — shows a driver's **rank and gap-to-leader within one map's ranked leaderboard**. Doesn't fit here: Latest Laps rows span many different maps and players, not one ranked list.
- Player Single's inline modal — shows **one lap in isolation against its map's course record** ("vs MAP RECORD"), with a "VIEW MAP LEADERBOARD" link. This is the right shape for Latest Laps too (a feed of laps across different maps), but it was duplicated inline in `player-show.blade.php` rather than being a shared partial.

**Extracted Player Single's inline modal into `resources/views/livewire/partials/lap-vs-record-modal.blade.php`** — this is the "second real consumer" extraction trigger the project already follows for `podium.blade.php` and `HasLapDetailModal` itself. Player Single now `@include`s it instead of carrying its own copy. Made two fields optional in the extraction (`ping`, `recordTag`) since real data doesn't have either — the real `lap_times` schema has no `ping` column at all (that was mock-only flavor), and there's no clan/tag concept in the real schema (`players` has no tag field, matching the already-documented "clans out of scope" decision). The partial now renders those lines conditionally rather than assuming they're always present.

**Wired into `ServerShow`**: added `HasLapDetailModal` to the component, and a new public `$latestLaps` array holding the *current page's* transformed rows (so the modal can index into exactly what was rendered, regardless of which page of pagination is showing). `laps()` now also computes each map's course record for real — a cheap bulk query (`whereIn('map_id', ...)->get()->groupBy('map_id')->map(fn ($g) => $g->sortBy('time')->first())`) rather than mock — giving the modal's "vs MAP RECORD" line real data for the first time anywhere in the app; the split-by-split comparison itself stays mock everywhere (real per-checkpoint comparison isn't built yet, unrelated to this change). Each Latest Laps row was converted from a plain `<div>` to a `<button wire:click="openLap($index)">` with the same hover-accent styling used for every other clickable row in the app.

**Caught and fixed before landing**: the shared partial's `$sel = $laps[$selectedDriverIndex] ?? $laps[0]` assumed `$laps[0]` always exists — true for every existing mock dataset, but not for real data (`RoutesTest`'s factory-seeded server has zero laps). Fixed to `$laps[$selectedDriverIndex] ?? $laps[0] ?? null`, wrapping the whole modal in `@if ($sel)` so it compiles safely even when nothing exists to show (the modal can never actually be *opened* on an empty list — nothing renders an `openLap()` trigger for it — but the Blade template still has to render without erroring either way). This is what `RoutesTest`'s `servers.show` case caught immediately.

## Split comparison built for real on Server Single

The user noticed the split rows in the Lap Detail modal never changed between different laps ("they all seem the same") and that the last row looked visually broken. Both were real, correctly-diagnosed symptoms: `HasLapDetailModal::getComparisonProperty()` returns a single hardcoded array of 14 mock rows regardless of which lap is selected — always the same numbers — and that mock data's last row (SEC 14) has a `barW` of 88 against every other row's 6–22, making its comparison bar look wildly, inconsistently larger for no real reason.

Given `ServerShow` already has real `LapTime`/`LapTimeSplit` models available (unlike `PlayerShow`, still fully mock), implemented **real per-checkpoint split comparison** rather than just patching the mock data to look less broken:

- `ServerShow::getComparisonProperty()` overrides the trait's mock method — a plain method on the using class takes precedence over a same-named trait method in PHP, so no changes were needed to `HasLapDetailModal` itself or its other consumers (`PlayerShow`, `MapLeaderboard`, `ServerMapLeaderboard`), which keep the mock fallback until they're wired to real data too.
- For the selected lap, fetches its `LapTimeSplit` rows and the map's course-record lap's splits, matches them by `checkpoint_id` (confirmed via real data that checkpoint IDs are consistent within a map across different laps), and computes real per-split deltas, a running cumulative delta, and a faster/slower color per split.
- **Bar width is now relative, not hardcoded**: scaled to the largest delta actually present in that specific comparison (`barW = abs(delta) / maxAbsDelta * 100`), so a comparison's visual proportions always make sense relative to itself — there's no way to reproduce the old mock's "one row is inexplicably 4x bigger than the rest" problem, since the bars are always derived from the real numbers being shown.
- **Added `lapId`/`recordLapId` to each Latest Laps row** so the modal can look up the right two laps' splits by ID (not by the position hack elsewhere) once opened.

**Real data reality check**: only ~4% of real laps (64 of 1657) have any split rows recorded at all, and — checked directly — there is currently no pair in the dataset where *both* a lap and its map's record-holder lap have splits simultaneously. So in practice, today's real data almost always produces an empty comparison. This isn't a bug in the new logic (verified separately: manually comparing two real laps that do both have splits, e.g. laps 1877 and 1878 on map 1, produces correct, sane per-checkpoint deltas) — it's just sparse source data. Added a "No split data available for this lap" message to `lap-vs-record-modal.blade.php` for exactly this case, replacing what would otherwise be a table with headers and no rows.

**Extracted the comparison math into `LapTimeSplit::compare(int $lapId, int $referenceLapId): array`** immediately after building it, once it became clear the very next page (`ServerMapLeaderboard`, see below) needed the identical logic — rather than copy-pasting the delta/running/barW calculation a second time into a different component.

## Server Map Leaderboard wired to real data (nested per-server, per-map leaderboard)

Continuing Phase 2 item 8, "next view" after Server Single: `ServerMapLeaderboard` (`/servers/{serverId}/maps/{mapId}`) — the nested leaderboard reached by clicking a map from Server Single's Maps table. Rewrote `mount()` to build the ranked leaderboard for real: one row per distinct player's best lap on this server+map (ties broken by earliest lap, matching the tie-break precedent already established in [global-ranking.md](global-ranking.md)), with a real gap-to-leader, real relative dates, and real total-lap count for the page's badges. Reused `LapTimeSplit::compare()` for the split-pace comparison against the #1 driver's lap — same pattern as Server Single, no duplicated math.

**Two more mock artifacts were caught and fixed while wiring this page**, both in the *other* pre-existing modal partial (`lap-detail-modal.blade.php`, the rank-based leaderboard variant — distinct from `lap-vs-record-modal.blade.php`) that this page (and the still-mock global `MapLeaderboard`) share:
- A hardcoded **"▲ Gained in SEC 1 · ▼ Lost most in SEC 2"** line that never reflected the actual selected driver's comparison — always said SEC 1/SEC 2 regardless of what the real data showed. Replaced with a real computation (`collect($this->comparison)->sortBy('delta')->first()` / `sortByDesc('delta')->first()`), hidden entirely when there's no comparison data to derive it from (mock pages using the trait's default, or a real lap with no split coverage).
- A **"VIEW DRIVER" button that was never a real link** (a plain non-clickable `<div>` styled like a button) in the original mock. Now renders as a real `<a href="{{ route('players.show', ...) }}">` when the selected driver has a real `playerId`, falling back to the old non-functional `<div>` for pages (`MapLeaderboard`) that don't have real player identity wired up yet.

Also fixed the `SHOWING X / 128 LAPS` footer in the shared `leaderboard-podium-and-table.blade.php` partial — "128" was a hardcoded mock constant regardless of the actual driver count. Now accepts an optional `$totalLaps` variable (falls back to `128` when not passed, so the still-mock `MapLeaderboard` is unaffected); `ServerMapLeaderboard` passes the real total lap count for this server+map.

**Same empty-array bug pattern struck a second modal partial**: `RoutesTest`'s `servers.maps.show` case (now needing a real `Map` row too, not just `Server` — added `Map::factory()->create(['id' => 1, ...])` to the shared `beforeEach()`) immediately surfaced the exact same `$drivers[0]` assume-non-empty bug in `lap-detail-modal.blade.php` that had already been fixed once in `lap-vs-record-modal.blade.php`. Fixed identically: `$drivers[$selectedDriverIndex] ?? $drivers[0] ?? null`, wrapped in `@if ($sel)`. Also updated the nested-leaderboard eyebrow-text test to assert against the real factory-seeded server name ("Test Server Alpha") instead of the old hardcoded mock string ("HRL · DEMO SERVER #2"), which no longer appears anywhere now that the component reads the real `Server` model.

## Split comparison refined: three real bugs, one UX gap, all fixed

After using `ServerMapLeaderboard`, the user reported four specific issues with the split comparison — all real, all fixed in `LapTimeSplit::compare()` and the two modal partials:

1. **"If the current lap has splits, but the #1 lap does not, show the splits but no delta."** Previously `compare()` returned an empty array (nothing shown) whenever *either* lap lacked splits — even when the selected lap itself had real data worth displaying. Changed: only return `[]` when the *selected* lap has no splits at all (nothing to show, period). If the selected lap has splits but the reference lap doesn't, return the raw split rows with a new `hasReference: false` flag and null delta/running/bar fields. Both modal partials now render those rows as plain split times (no delta column, no bar, an em-dash in the gap column) and show an explanatory note below the table: "This lap has split data, but the reference lap doesn't — showing raw splits, no delta comparison."

2. **"There's a bug in deltas, as offset from center, it should never be 100%? If 100% its actually 50%, correct? As it's half."** Correct diagnosis of a real bug: the delta bar is drawn from a center reference line (`left-1/2`/`right-1/2` CSS positioning) out to one edge, so it only ever occupies **half** the track's total width — but `barW` was being scaled up to a max of 100, meaning the "biggest delta in this comparison" row would render with `width: 100%` starting from the center line, overflowing 50% past the track's actual edge. Fixed: `barW` is now scaled to a max of **50** (`min(50, absDelta / maxAbsDelta * 50)`), so the largest delta in any comparison reaches exactly the track's edge and never overflows. This is normalized (relative to that comparison's own max), clamped (capped at 50), and symmetric around the center line by construction (the `faster`/`slower` branch already picks which side to grow from) — matching the three properties the user asked for explicitly.

   **Also fixed the same overflow bug in the mock data** (`HasLapDetailModal::getComparisonProperty()`, still used by `PlayerShow` and the global `MapLeaderboard`) — its old `barW` values (6–22, with one outlier at 88) were tuned for the old buggy 0–100 scale. Rescaled proportionally to fit under the corrected 0–50 max (88 → 45, others scaled down to match) so the same visual bug doesn't persist on pages still running mock data.

3. **"When looking at the current #1 lap, compare it to position #2."** Previously, opening the #1 driver's own lap in `ServerMapLeaderboard` called `compare($lapId, $topLapId)` with `$lapId === $topLapId` (comparing #1 to itself), which `compare()` correctly refuses (returns `[]`) — but that left #1's own modal view with nothing to show at all. Fixed: when the selected driver's lap *is* the #1 lap, the reference becomes driver #2's lap instead. `ServerShow`'s Latest Laps has an analogous case (viewing a lap that already *is* its map's course record) — extended the same fix there, comparing against that map's runner-up (2nd-fastest) lap instead of the record lap when they'd otherwise be identical.

4. **Found and fixed while implementing #3**: the earlier "biggest gain/loss sector" computation (`docs/decisions.md`'s Server Map Leaderboard entry) sorted on the **formatted delta string** (`"+0.041"`, `"−0.028"`) via `collect(...)->sortBy('delta')` — a lexicographic string sort, not a numeric one, so it could easily pick the wrong "biggest" row. Added a `deltaValue` field (signed float, not display-formatted) to every comparison row specifically for sorting/comparison purposes, and switched the gain/loss computation to sort on that instead, filtering out `hasReference: false` rows first (their `deltaValue` is null).

All four fixes verified against real data: `LapTimeSplit::compare(1877, 1878)` now caps at `barW: 50` (was `100`); `compare(1877, <lap-with-no-splits>)` returns rows with `hasReference: false`; viewing `ServerMapLeaderboard`'s #1 driver now correctly returns 5 real comparison rows against #2 instead of an empty array; and the equivalent record-vs-runner-up fallback on `ServerShow` was confirmed the same way.

## The "#1 vs #2" fallback had a real display bug: the label lied about what was compared

Testing the #1-vs-#2 fix against real data (server 7, map 1: driver #1 = lap 1934, which has splits; driver #2 = lap 1868, which has none) surfaced the actual root cause of what looked like a wrong/reversed message: **the "vs RECORD" label in both modal partials always displayed driver #1's own name and time, regardless of which lap the comparison had actually used.** When viewing #1's own lap, `getComparisonProperty()` had already silently swapped the reference to #2 (per the earlier fix) — but the header still said "vs RECORD · [Æ] kingas 1:03.43" (driver #1 naming *themselves* as "the record" they're being compared to), while the actual comparison beneath it was against #2. The "This lap has splits, but the reference lap doesn't" message was technically accurate for the *real* comparison (vs #2), but directly contradicted the header line claiming the comparison was "vs RECORD" (i.e., against themselves) — which is what read as backwards.

**Fixed by making the reference display dynamic instead of hardcoded**: added `getComparisonReferenceProperty()` returning `['name', 'time', 'label']` for whichever lap was actually used — `ServerMapLeaderboard` and `ServerShow` each override it to mirror their own `getComparisonProperty()` fallback logic (extracted into a shared private `resolveComparisonReference()` in each, so both methods can't drift out of sync with each other). Both modal partials now read `$this->comparisonReference` instead of a hardcoded `$p1 = $drivers[0]` / `$sel['recordHolder']`, and the "GAP TO RECORD" label text and "no delta comparison" explanatory message both use the dynamic reference's name too, so every piece of text in the modal agrees about which lap it's actually being compared to.

`HasLapDetailModal`'s trait default (`$drivers[0]`, labeled "RECORD") covers `MapLeaderboard`'s still-mock case, guarded with `property_exists($this, 'drivers')` so it doesn't break `PlayerShow` (which has no `$drivers` property, uses `$laps` instead) — for that partial (`lap-vs-record-modal.blade.php`), the blade falls back to the selected lap's own mock `recordHolder`/`recordTime` fields when `$this->comparisonReference` is null, preserving `PlayerShow`'s exact original (correct, since its mock data has no self-comparison edge case) behavior.

**Also fixed while at it**: `ServerMapLeaderboard`'s "GAP TO RECORD" figure for driver #1 was previously always `"—"` (nonsensical — you can't have a gap to your own record). Per the user's request ("When looking at #1, the Gap to Record should be compared to #2 as per splits"), #1 now shows a real gap to #2, framed as a lead (`"-1.600"`) rather than a deficit. The "GAP TO ..." label itself is also dynamic now (reads "GAP TO RUNNER-UP" when viewing #1, "GAP TO RECORD" otherwise), matching whichever comparison is actually shown below it.

Verified against real data end-to-end: viewing #1 (lap 1934) now shows "vs RUNNER-UP · ©opyrite", a `-1.600` gap, and the correctly-directed "this lap has splits, but ©opyrite's doesn't" message; viewing #2 (lap 1868, no splits at all) correctly shows the plain "No split data available" message instead, since #2's *own* lap has no splits to display in the first place.

## Gap display: always "+", colored by direction not by who's viewing

Two follow-up refinements: **"all 'gap to' should render as +..."** — #1's gap-to-runner-up had been shown with a leading `-` (framed as a lead) while every other row's gap-to-record used `+` (framed as a deficit); inconsistent signs across rows. Changed #1's gap to also use `+`, so every gap value in the leaderboard and modal is a plain `+X.XXX` magnitude regardless of direction — direction is now communicated by color instead of sign.

**"If gap to runner up, show as positive colour instead of red"**: the "GAP TO X"/"FINAL DELTA" values were hardcoded `text-hud-red` unconditionally — correct for everyone comparing against the record (a deficit, i.e. "behind"), wrong for #1 comparing against the runner-up (a lead, i.e. "ahead" — a good thing, and this app already uses green for every other "faster/better" indicator). Added a `$gapColorClass` computed once per modal render from `$reference['label']` (`'RUNNER-UP'` → green, anything else → red) and applied it to both the "GAP TO X" value and the "FINAL DELTA" value, so they always agree with each other and with the dynamic label above them.

While making this change, also caught and fixed a regression: the "vs RECORD" line had reverted to hardcoding `{{ $p1['name'] }}` — `$p1` isn't defined anywhere in this file anymore (replaced by `$reference` when the dynamic-reference fix landed), so this would have thrown an undefined-variable error the next time anyone opened this modal. Restored the `@if ($reference) vs {{ $reference['label'] }} · {{ $reference['name'] }} {{ $reference['time'] }} @endif` version.

Verified against real data: driver #1's gap is now `+1.600` (was `-1.600`) and renders in green; driver #2's gap is `+1.600` and renders in red; both pages render successfully.

## Hover-accent rows missing left padding, fixed app-wide

The user flagged Recent Laps' rows: no gap between the row's text and the left hover-accent line (`border-l-2 border-l-transparent` → `hover:border-l-hud-green`), and suggested matching the Fav Servers table style instead. Rebuilt Recent Laps as a proper desktop-grid/mobile-list pair (like Fav Servers and every other table in the app) with `px-3.5` on both.

Given the explicit "always do this" instruction, audited the whole app for the same pattern rather than treating it as Recent-Laps-specific — found the identical missing-padding bug on **every mobile-list row in the project**: Map List, Player List, Server List, Server Single's Maps table, and the Map Leaderboard's rest-table (desktop grid tables already had `px-3.5`; only the mobile flex-row variant was missing it everywhere, apparently copy-pasted without it from the start). Asked whether to fix just Recent Laps or everywhere — **user chose everywhere**. Added `px-3.5` to all of them in one pass. Documented as a standing rule in [coding-standards.md](coding-standards.md): any row using this hover-accent pattern needs `px-3.5` from the moment it's written, not as an after-the-fact fix.

While in this file, also added `serverId` to Player Single's Fav Servers mock data and turned its rows into links to `servers.show`, per explicit request — previously the section showed server names as plain text with no way to navigate to that server's page, inconsistent with every other server-name reference in the app.

## Status-dot badges misaligned app-wide, fixed with `-mt-px`

The user reported "badges with a dot are not aligned properly throughout the app (its slightly lower)." Found exactly two status-dot badges in the app — the nav's "LIVE" indicator (`components/layout.blade.php`) and the featured server's "ONLINE · {region}" badge (`server-list.blade.php`) — both `inline-flex items-center` wrapping a small `rounded-full` dot next to all-caps mono text. `items-center` centers the dot geometrically against the text's line-height box, but all-caps text has no descenders, so its visual weight sits above true vertical center — a geometrically-centered dot reads as slightly low next to it. Fixed both with `-mt-px` on the dot span (nudges it up 1px), and documented the fix as a standing rule in [coding-standards.md](coding-standards.md) so any future dot-indicator badge gets it right immediately rather than needing the same fix rediscovered later.

**Immediate correction**: the user actually meant the `◆` diamond glyph leading badges like "◆ MOST ACTIVE" and "◆ TOP SCORE" — a different element, same underlying font-metrics cause (all-caps text has no descenders, so a geometrically-centered glyph next to it reads low). Found four instances: `server-list.blade.php`'s "◆ MOST ACTIVE", `leaderboard-podium-and-table.blade.php`'s "◆ COURSE RECORD", and two badge strings baked into mock data (`ServerShow.php`'s "◆ TOP SCORE", `PlayerList.php`'s "◆ #1 GLOBAL") rendered through `podium.blade.php`.

Fixed by wrapping just the glyph in `<span class="-translate-y-px inline-block">◆</span>`, not the whole badge string — and, since two of the four badges were plain PHP strings rather than Blade markup, **moved the `◆` out of the mock data entirely** so the fix only has to live in one place. `podium.blade.php`'s 1st-place badge rendering now prepends the glyph+wrapper itself; `ServerShow.php`/`PlayerList.php`'s `'badge'` values are now just the label text (`'TOP SCORE'`, `'#1 GLOBAL'`) with no `◆` baked in. This is also a small consistency win independent of the alignment fix — badge presentation markup no longer lives partly in PHP data arrays and partly in Blade.

Per [homepage.md](homepage.md) and the prior decision above, the six candidate highlight blocks are real mock data keyed by type in `Home::mount()`, run through the actual fixed-priority selection algorithm (`collect($priority)->map(...)->filter(...)->take(3)`) — not hardcoded to "whichever 3 looked good." One block (**Fastest Improvements**) was deliberately mocked as empty this round specifically to prove the fallback works: verified by rendering the route and confirming Records + Most Active Server (both non-empty, top 2 priority) show, Fastest Improvements is skipped, and New Maps/Servers (priority 4) fills the third slot instead. This is the first page in the project where the mock data exists to exercise real branching logic, not just populate a static layout.

Each of the 6 block types got its own partial under `resources/views/livewire/partials/highlights/` (`records`, `most-active-server`, `fastest-improvements`, `new-content`, `achievements`, `live-stats`), included dynamically by type name (`@include('livewire.partials.highlights.' . $highlight['type'], ...)`) rather than one giant `@switch` in `home.blade.php`. The Most Active Server block reuses the 1st/2nd/3rd green/cyan/gold color convention from the existing lap-leaderboard podium but does **not** yet share a component with it — podium partial extraction (roadmap Phase 1, item 5) is still pending and is now motivated by a third real use case (this block, plus the still-unbuilt Players List podium).

Quick Stats (always-shown all-time totals) and Quick Links (static Servers/Maps/Players nav) were built as fixed sections outside the highlight-selection system, per spec.

## `LapTimeSplit::compare()` was only one-way, then its fix had its own display bug

`compare($lapId, $referenceLapId)` originally handled "selected lap has splits, reference doesn't" (returns the selected lap's raw times, `hasReference: false`) but not the mirror case — if `$mySplits` was empty it returned `[]` immediately without ever checking whether the *reference* lap had splits. Real split coverage is sparse (~4% of laps), so "reference has splits, selected lap doesn't" isn't an edge case, it's routine — e.g. lap #1868 (0 splits) ranks #2 on its server+map leaderboard, compared against the #1 lap (5 splits).

**Fix, part 1 (model)**: added the missing branch — when the selected lap has no splits but the reference does, return the reference's raw checkpoint times, tagged with a new `usingReferenceSplits` flag (distinct from `hasReference`, since that alone can't say *which* side the raw numbers actually belong to). Also fixed a latent bug this exposed: the view's `collect($this->comparison)->every(fn ($c) => ! $c['hasReference'])` check is vacuously `true` on a genuinely empty array too, so "neither lap has splits" (should show "No split data available") and "selected lap has splits, reference doesn't" (a real message) were indistinguishable — now gated behind `count($this->comparison) > 0` first, then split into two explicit `usingReferenceSplits`-based branches.

**Fix, part 2 (view, found immediately after part 1 shipped)**: the reference lap's raw times were rendered in the same bold "myTime" slot used for genuinely-mine data — visually indistinguishable from a real comparison despite the explanatory caption below the table. Reported as "the view that has no splits is still showing splits? I think it's actually showing the compared splits" while looking at lap #1868. Fixed by branching the row template on `usingReferenceSplits`: when true, the primary bold slot shows `—` (this lap has nothing) and the reference's number moves to the small muted "REC" line instead — mirroring how a real comparison always shows the reference's time in that exact position. Applied to both `lap-detail-modal.blade.php` and `lap-vs-record-modal.blade.php` (same structure, same bug, same fix).

Added `tests/Feature/LapTimeSplitCompareTest.php` covering all four branches (same-id no-op, neither-has-splits, real comparison, each one-sided direction) — this method had zero test coverage before either fix.

## Servers List wired to real data — mock fields without a real equivalent are dropped, not faked

`ServerList` was mock (region badge, live online/offline dot, players-now/max capacity bar, ping). Wiring it to real `Server`/`LapTime` data meant confronting that several of those fields have **no real-schema equivalent at all** — the decision was to drop or honestly re-derive each one rather than invent a plausible-looking fake value:

- **`region`**: dropped entirely. No column exists on `servers`, and there's no honest way to derive it from other real data.
- **Live online/offline status**: no heartbeat or live game-server query exists. Replaced with a recency proxy — "online" means "has a lap recorded in the last 24 hours." Labeled "ACTIVE" in the UI (not "ONLINE") to avoid implying a real-time check that isn't happening. The featured card's badge is now conditional on this flag instead of always showing "ONLINE."
- **"Now playing" map**: no `current_map_id` column. Derived from each server's most recent `lap_times` row's map, per the approach already documented in [database.md](database.md) before any wiring happened.
- **Player capacity bar (`players_now`/`players_max`)**: no capacity columns exist. The bar is repurposed to show this server's distinct-player count *relative to the busiest server in the current list* — a real comparison, not a fabricated capacity percentage.
- **Ping**: dropped entirely per explicit request — no per-server ping data exists (only individual lap submissions might carry it in a future webhook payload, and even then it'd be per-lap, not per-server).
- **"Most active" (featured server)**: real data, but a **deliberately simple proxy** — ranked by lap count in the last 30 days, tie-broken by most recent lap — not the full Activity Score algorithm from [most-active-server.md](most-active-server.md) (item 12, needs a scheduled recalculation job and isn't built). Chosen because the real data's date range (through 2026-07-01) actually has laps within a 30-day window, unlike a 24-hour window which would zero out the whole list.
- **Header stats**: "N ONLINE" counts servers passing the 24h-recency proxy; "N PLAYERS" is `LapTime::distinct('player_id')->count()` (players who've actually recorded a lap, not the raw `players` table count, which includes profiles with no real activity); "N LAPS TODAY" is a literal `whereDate('created_at', today())` count — genuinely 0 most days given the real data's date range, and left honestly at 0 rather than dressed up.
- Removed the "LAST SYNC · 12s AGO" footer text — it implied a live polling mechanism that doesn't exist.

Added `tests/Feature/ServerListTest.php` asserting the real derivations (now-playing map, 24h online proxy via `Livewire::test()`, distinct-player counting) rather than just that the route renders.

## Maps List wired to real global data — zero-lap maps hidden after review

`MapList` was the next Phase 2 wiring target after Servers List. Unlike the server-list mock, all three visible map fields have direct real-schema equivalents: display name, global lap count, and global best lap. No mock-only UI field needed to be fabricated or removed.

- The UI uses `maps.label`, not `maps.name`: real data confirms `name` is a machine slug (`bloodgulch`, `dangercanyon`) while `label` is the intended public text (`Bloodgulch`, `Danger Canyon`).
- Lap count and best time are global across every server, computed in one left-joined aggregate query (`COUNT(lap_times.id)` and `MIN(lap_times.time)`) rather than one query per map.
- **Initial decision, reversed after review**: the first real-data pass used a left join so maps with no lap history stayed visible with `0 LAPS` and an em dash. The user then chose to hide them: Maps List is an entry point to global leaderboards, so a map with no laps has no useful destination. The query now uses an inner join and excludes zero-lap maps at the database level. Real map 10 (`Bloodgulch - Any Order`) currently exercises this exclusion.
- Maps are ordered alphabetically by their public label for a stable, user-facing list; raw database insertion order has no display meaning.

Added `tests/Feature/MapListTest.php` covering the all-server aggregation, public-label choice, formatted real best time, and zero-lap exclusion.

## Source control initialized at the application root

The rebuild previously had no usable Git repository. Initialized a local repository with `main` as the default branch directly in `www/redesign.hrl.effakt.info/`, not in the surrounding hosting-account home directory — the latter contains unrelated account configuration, caches, logs, and other sites/tools that must never become part of HRL's history.

The intended GitHub name is `EffakT/HRL-Web-Interface-v2`: it clearly distinguishes this Laravel/Livewire rebuild from the legacy `HRL-Web-Interface` repository without pretending it is an in-place continuation of the old framework history. GitHub's repository API returned 404 for that candidate at setup time, so the name appeared available.

No files were staged or committed, and no remote was created yet. The server has Git but no GitHub CLI, global commit-author identity, or configured credential helper. Repository visibility and licensing also need an explicit decision. Recommended sequence: create it private, review the exact first-commit file set and replace the stock Laravel README (done), choose a license, then make it public later if desired. `.gitignore` was verified to exclude the real `.env`, `vendor`, `node_modules`, and compiled `public/build` assets before any commit.

**Follow-up**: the user created `EffakT/HRL-Web-Interface-v2` as an empty public repository and supplied its SSH URL, now configured locally as `origin`. GitHub host-key trust was established with SSH's `StrictHostKeyChecking=accept-new`; authentication then failed with `Permission denied (publickey)`, confirming this server has no GitHub-authorized account/deploy key yet. The initial commit and push remain deliberately pending rather than switching to a token-bearing URL or exposing credentials. A commit-author name/email and license choice are also still needed.

Generated a dedicated Ed25519 deploy key at `/var/www/redesign_hrl_usr54/data/.ssh/hrl_web_interface_v2` (outside the application Git root) and pinned this repository's local `core.sshCommand` to it with `IdentitiesOnly=yes`. This avoids sharing a personal account key or putting a token in a remote URL. The public half must be added to this GitHub repository with write access before pushing; the private half stays server-local. Staged all 165 intended project files and ran index-level secret checks: no `.env`, private key, dependency directory, compiled build, GitHub token, or non-placeholder credential is staged. Matches in `.env.example` and the checked-in Laravel skill documentation are examples/placeholders only.

**Deploy-key authentication confirmed**: after the user added the public key with write access, `git ls-remote origin` completed successfully against the empty repository. SSH setup is complete; no token or personal account key is involved.

**Initial-history choices resolved**: repository-local commit identity is `EffakT <dusaro123@gmail.com>`. The user explicitly chose no license for now; because the repository is public, the source is visible after push but has no explicit open-source permission grant unless that decision changes later.

## Global Map Leaderboard wired to real data — archived servers remain historical context

`MapLeaderboard` now ranks real laps across all servers: sort by time, then `created_at`, then id; take the first lap per `player_id`. This yields one global PB per player, preserves the established earliest-achievement tie-break, and uses id as the final deterministic fallback for historical rows sharing day-only timestamps. The page's lap count remains the raw number of attempts; its ranked-driver count is shown separately.

Mock clan tags and ping were removed because neither exists in the real schema. Global rows instead show the real server where the player's PB was achieved, and the modal links to that server when it is active. The shared fake pagination text was removed rather than implying controls that do nothing. Real dates use relative display plus exact UTC hover tooltips, following the existing date convention.

The first real `/maps/1` render exposed a non-obvious soft-delete issue: 54 Bloodgulch laps reference servers 3/4, both archived. `LapTime::server()` originally respected `Server`'s soft-delete scope and hydrated `null`, fatal-erroring when the leaderboard read the server name. Decision: global history includes these valid laps; server archival does not erase their provenance. `LapTime::server()` now includes trashed rows, the UI adds `ARCHIVED`, and no dead link to the active-only server route is rendered.

The global Lap Detail modal now uses `LapTimeSplit::compare()` with the same dynamic reference rule as the nested leaderboard: everyone compares to #1, while #1 compares to #2. Added `MapLeaderboardTest.php` covering cross-server PB selection, global scoping, time/date tie-breaking, archived-server history, active server links, and real split comparison.

**Immediate reversal after review — archived means nonexistent**: the initial pass retained laps from soft-deleted servers, labeled their server `ARCHIVED`, and suppressed the dead server link. The user rejected that product behavior: “If trashed, treat as it doesn't exist.” Reverted `LapTime::server()` to the normal soft-delete scope and excluded archived-server laps from all global map reads: Map List counts/bests, Map Leaderboard attempts/rankings, and Server Single's global course-record reference. The archived-server test now asserts exclusion rather than historical display. Verified against real Bloodgulch data: the page moved from 182 attempts / 145 ranked drivers (including archived servers) to the correct active-only 128 attempts / 120 drivers.

**Podium split empty state**: when the #1 lap has no split rows (common with ~4% coverage), the podium previously rendered a hollow split bar under “0 CHECKPOINTS,” which looked like a broken visualization. It now shows the same explicit “No split data available for this lap.” message as the Lap Detail modal. The shared podium partial applies this to both global and nested leaderboards.
